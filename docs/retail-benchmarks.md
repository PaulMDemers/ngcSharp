# Retail Benchmark Notes

The local retail benchmark images are user-provided RVZ files in the workspace root:

- `Sonic Adventure 2 - Battle (USA) (En,Ja,Fr,De,Es).rvz`
- `Pikmin (USA).rvz`
- `Mario Kart - Double Dash!! (USA) (Debug).rvz`

Use short bounded diagnostics first, then raise the instruction budget only when the previous artifact explains the next blocker.

## Current Commands

```powershell
dotnet run --project src/NgcSharp.App/NgcSharp.App.csproj --no-restore -- diagnose-disc "Sonic Adventure 2 - Battle (USA) (En,Ja,Fr,De,Es).rvz" --max-instructions 3000000 --snapshot-interval 250000 --out artifacts/retail-smoke-display-scale/sonic --name sonic-display-scale
dotnet run --project src/NgcSharp.App/NgcSharp.App.csproj --no-restore -- diagnose-disc "Pikmin (USA).rvz" --max-instructions 3000000 --snapshot-interval 250000 --out artifacts/retail-smoke-display-scale/pikmin --name pikmin-display-scale
dotnet run --project src/NgcSharp.App/NgcSharp.App.csproj --no-restore -- diagnose-disc "Pikmin (USA).rvz" --max-instructions 12000000 --snapshot-interval 1000000 --out artifacts/retail-smoke-strcmpff2/pikmin-12m --name pikmin-strcmpff2-12m
```

## Dolphin Reference Frames

The local Dolphin build can be used as a visual oracle for short retail boot windows:

```powershell
powershell -ExecutionPolicy Bypass -File scripts/generate-dolphin-reference.ps1 -Seconds 16
```

The script creates a timestamped run under `artifacts/dolphin-reference`, using a disposable Dolphin user profile with PNG frame dumping enabled. It keeps sampled PNGs plus a contact sheet for each benchmark game and removes the full per-frame dump unless `-KeepFullDump` is supplied. Use `-Games sonic` or `-Games pikmin` for a targeted longer reference pass.

The first generated reference run is `artifacts/dolphin-reference/20260517-125741`:

- Sonic Adventure 2 Battle (`GSNE8P`): 462 Dolphin frames; samples show the Nintendo license, Sega splash, and Sonic Team splash.
- Pikmin (`GPIE01`): 384 Dolphin frames; samples show the Nintendo splash and title screen.

The Sonic-only reference run `artifacts/dolphin-reference/20260526-101428` captures 2,101 Dolphin frames over 60 seconds, and its samples include the city/bridge intro region at frame 600. A finer run, `artifacts/dolphin-reference/20260526-101627`, samples frames 500-850 around that sequence.

Compare the current emulator output against the latest Dolphin reference run with:

```powershell
powershell -ExecutionPolicy Bypass -File scripts/run-retail-reference-compare.ps1 -NoBuild
```

The comparison harness writes `our.png`, per-sample `diff.png`, and a summary CSV under `artifacts/retail-reference-compare/<timestamp>`. Use `-RecompareRunRoot <run-dir>` to regenerate diff reports from an existing emulator run without spending time rerunning the games.

The first comparison run is `artifacts/retail-reference-compare/20260517-131419`:

- Sonic at 50.5M instructions still presents the in-game "No Memory Card in Slot A" prompt, while Dolphin's same boot window reaches the Sega/Sonic Team splash sequence. This is a boot-state mismatch first, not yet a same-scene rendering diff.
- Pikmin at 12M instructions produces a full-frame gray diagnostic/display-copy image, while Dolphin reaches the Nintendo splash/title sequence. The next Pikmin target is therefore presentation/copy source selection and boot progression rather than TEV detail.

## Sonic Visible Frame Comparison

Use this pair to compare the draw-time texture snapshot path against final-RAM texture sampling on the 50.5M Sonic window:

```powershell
dotnet run --project src/NgcSharp.App/NgcSharp.App.csproj --no-restore -- run-disc "Sonic Adventure 2 - Battle (USA) (En,Ja,Fr,De,Es).rvz" --max-instructions 50500000 --fast-forward-idle --dump-gx-frame artifacts/sonic-last-nonblack-display-copy-no-auto/frame.png --gx-frame-source last-nonblack-display-copy --gx-frame-max-draws 900 --gx-frame-max-raster-pixels 12000000 --gx-disable-auto-texture-snapshots --no-registers
dotnet run --project src/NgcSharp.App/NgcSharp.App.csproj --no-restore -- run-disc "Sonic Adventure 2 - Battle (USA) (En,Ja,Fr,De,Es).rvz" --max-instructions 50500000 --fast-forward-idle --dump-gx-frame artifacts/sonic-last-nonblack-display-copy-auto/frame.png --gx-frame-source last-nonblack-display-copy --gx-frame-max-draws 900 --gx-frame-max-raster-pixels 12000000 --no-registers
dotnet run --project src/NgcSharp.App/NgcSharp.App.csproj --no-restore -- compare-images artifacts/sonic-last-nonblack-display-copy-no-auto/frame.png artifacts/sonic-last-nonblack-display-copy-auto/frame.png --diff artifacts/sonic-last-nonblack-display-copy-auto/diff-vs-no-auto-cli.png
```

Expected current comparison result:

- `43,997` changed pixels (`14.322%`)
- baseline nonblack: `53,388` pixels, bounds `130,177-639,316`
- auto-snapshot nonblack: `51,571` pixels, bounds `130,177-639,302`
- diff artifact: `artifacts/sonic-last-nonblack-display-copy-auto/diff-vs-no-auto-cli.png`

## 2026-05-15 Baseline

After the display-copy scale/filter pass:

- Sonic at 3M instructions reaches VI/SI/AI/DI setup and thread scheduling, but no GX FIFO writes are captured yet. The tail is in startup math/audio work around `0x801114xx`/`0x801116xx`/`0x801183xx`.
- Pikmin at 3M instructions writes VI framebuffer registers and GX setup state, but has no draw command in the captured FIFO yet.
- Pikmin at 12M instructions reaches real GX quads. Before cache-loop fast-forward it captured 191,838 FIFO bytes and 736 quads, ending inside a `dcbi; addi +0x20; bdnz` cache maintenance loop.

After cache maintenance fast-forward:

- Pikmin at the same 12M instruction budget captures 242,543 FIFO bytes and 901 quads.
- The run reports `Fast-forwarded 1050585 cache maintenance instruction(s)`.
- The diagnostic GX frame is still mostly startup/clear geometry, but the benchmark now moves deeper per run.

After small leaf, memmove, and texture-sample helper fast-forwards:

- Pikmin at the same 12M instruction budget captures 284,924 FIFO bytes and 952 decoded draws.
- The latest run reports `Fast-forwarded 5752 small leaf helper instruction(s)`, `Fast-forwarded 12243330 memory copy instruction(s)`, and `Fast-forwarded 6831150 texture sample helper instruction(s)`.
- The current tail is around `PC=0x801F24D0`, and the hottest remaining profile cluster is the larger caller-side scan around `0x80027D3C..0x80027E68`, which repeatedly calls the texture-sample helper.

After null-terminated string copy and string compare fast-forwards:

- Pikmin at the same 12M instruction budget now lands later in allocator/accounting code around `PC=0x80047000`.
- The latest run reports `Fast-forwarded 9514 string copy instruction(s)` and `Fast-forwarded 5188668 string compare instruction(s)` in addition to the previous fast-forward buckets.
- This run captures 270,821 FIFO bytes and 557 decoded draws. The lower draw count versus the texture-sample-only run appears to be a phase change at the same interpreter budget rather than a FIFO regression: the run reaches different CPU code after skipping the repeated string-compare work.

After the small word-equality leaf fast-forward:

- A faster `run-disc` probe at 15M instructions, without PNG rendering, reaches object/list traversal around `PC=0x801B1328`.
- The run reports `Fast-forwarded 382332 small leaf helper instruction(s)`, up from 18,062 at the same 15M probe before the word-equality helper.
- The same 15M probe captures 354,037 FIFO bytes and 657 decoded draws. The tail repeatedly calls virtual methods while walking a linked list at `0x801B12EC..0x801B138C`, so it is not a safe broad fast-forward target.

After Sonic startup math/decode fast-forwards:

- The observed trig table at `0x80118300` writes `sin, cos` float pairs beginning at `0x802D7758`; the helper fills the remaining table and skips 11,796,482 startup instructions in the 10M/20M probes.
- Sonic's bit decode cluster around `0x800E1FA0..0x800E2178` is now partially collapsed by exact bit-scan and bit-unpack helpers. The 20M probe reports `Fast-forwarded 34339536 bit unpack instruction(s)`.
- The 20M Sonic probe now gets past the earlier decoder wall and a one-tick wait at `0x80117E0C`, then parks in a flag wait at `0x80022EF0..0x80022EF8`:
  `lwz r0, -31256(r13); cmpi r0,0; ble -8`.
- That flag wait is likely waiting for a subsystem event rather than CPU work. The next step is to identify the owner of `r13 - 31256` before forcing it; likely candidates are async DVD/resource load, audio init, or a scheduler flag.

After callback wait and dot-product fast-forwards:

- The `0x80022EF0` wait is tied to the OS callback slot at `r13 - 29932`, which points at `0x80022F5C`; that callback decrements the active flag at `r13 - 31256`. The exact wait helper now completes that pending callback count only when the registered slot matches the observed Sonic callback.
- The fixed dot-product loop at `0x80128DB8..0x80128F5C` now collapses 64-output blocks. The 20M Sonic probe reports `Fast-forwarded 1713855 dot product instruction(s)`.
- Sonic now reaches GX FIFO writes in the 20M probe. The current tail writes command byte `0x61` and a BP word through `0xCC008000` around `0x80104CEC..0x80104CF8`.

After the first Sonic GX capture and SI controller correction:

- A 20M Sonic GX diagnostic now renders a readable "No Memory Card in Slot A" prompt from the real FIFO stream. The capture in `artifacts/sonic-gx-20m/gx-frame.png` contains 937 decoded triangle-strip draws, 1874 rendered triangles, and 649,340 FIFO bytes.
- The prompt uses textured UI quads with RGB5A3 texture data and TEV modulate/pass-color stages, so the current renderer is no longer only clearing or drawing placeholder geometry.
- SI immediate transfers now use the communication-buffer command byte for controller `0x40/0x41` reads, return status/origin packets instead of stale type data, and acknowledge per-channel read-status data after the game consumes it. This fixes a serial-interrupt storm exposed by the more accurate controller response.
- Delayed input probes (`--controller-button-window a 20000000 24000000` and `--controller-button-window a 22000000 23000000`) both reach the same later CPU paths: at 24M the task-slot scan around `0x80125DEC..0x80125E18`, and at 30M the bitset dispatch around `0x800F3570..0x8010B37C`. The graphics FIFO is still dominated by repeated memory-card prompt draws in these windows, so "past the prompt" is not yet proven visually.
- GX frame dumping now has bounded render windows via `--gx-frame-skip-draws`, `--gx-frame-max-draws`, and `--gx-frame-max-raster-pixels`. A 24M delayed-input slice (`skip=900`, `max=25`, raster budget `500000`) completes and writes `artifacts/sonic-input-window-24m-raster-budget/gx-frame.png`; that particular slice is black, but the capture no longer hangs on large FIFO streams.
- GX frame sweeps can now be generated in one emulator pass with `--dump-gx-frame-sweep <dir> <start-skip> <step> <count>`. A 21M delayed-input smoke sweep wrote `artifacts/sonic-gx-window-sweep-smoke/gx-frame-sweep.csv` and two slices (`skip=0`, `skip=600`); the `skip=600` slice shows a few visible orange text fragments, so the next visible post-prompt signal is likely near that range. A heavier 24M sweep wrote four slices in `artifacts/sonic-gx-window-sweep-24m/` before manual stop, and its summary is flushed incrementally.
- Focused GX diagnostics show the prompt renderer is behaving in this region: a 21M slice at `skip=580`, `max=60`, raster budget `8000000` renders the full "No Memory Card in Slot A" panel cleanly in `artifacts/sonic-gx-window-skip580-focused/gx-frame.png`. Draw dumps at 21M/24M around skips `580..960` are still prompt-style pass-color bands plus RGB5A3 textured strips, not a new scene.
- Targeted SI tracing is now available with `--trace-si <csv-path>`. A 24M late-A run wrote `artifacts/sonic-si-late-a/si.csv`; during the late input window Sonic repeatedly writes `SIPOLL` and reads `SISR`, but does not read the channel input buffers, so it never samples the held A bit. A short held-A-from-boot trace wrote `artifacts/sonic-si-held-a-50k/si.csv` and confirms the current packet encoding does produce `0x01008080` when the game reads `SICxINBUFH`.

After the SI read-status correction and CTR loop fast-forwards:

- `SIPOLL` now marks enabled channels with the SDK-observed per-channel `0x20` read-status bit, and reading `SICxINBUFH/L` clears that bit. The late-A trace in `artifacts/sonic-si-late-a-rdst-set/si.csv` confirms Sonic reads all four channel input buffers during the prompt window and sees `0x01008080` while the A window is active.
- The old repeated-prompt FIFO pattern is gone after the SI fix. A 30M full-range GX render in `artifacts/sonic-gx-30m-post-si/gx-frame.png` is black, which likely means the prompt has been cleared and the next visible scene has not yet drawn within that budget.
- Added exact fast-forwards for `bdnz .` CTR delay loops and the Sonic decompressor's unrolled 8-byte CTR copy loop. The byte-copy helper was tightened to preserve forward overlap semantics after an initial snapshot-copy version exposed an invalid pointer path.
- A 35M Sonic probe with `--controller-button-window a 22000000 23000000` now completes cleanly and lands in math/update code around `0x800220F0..0x80022350`, with registers showing active data pointers in main RAM rather than the prior SI/task-scan loops.

After Sonic copy-loop fast-forwards and EFB-clear diagnostics:

- Added exact fast-forwards for Sonic's one-byte CTR remainder copy loop and the 32-byte unrolled word-copy loop at `0x8010C128..0x8010C16C`. The 50M Sonic probe now completes cleanly and tails in a bounded scan/list loop around `0x80116BBC..0x80116BE4`.
- A 50M GX sweep after the SI fix wrote black final-EFB images even though draw diagnostics found 2,076 decoded draws, including nonblack prompt/title-style textured strips. This showed the renderer was probably looking at the final cleared EFB after EFB-to-texture copies.
- Added diagnostic `--gx-frame-ignore-efb-copy-clear`, which leaves EFB contents intact after BP copy command `0x52`. With that option, the 50M Sonic capture at `artifacts/sonic-gx-50m-ignore-efb-clear/gx-frame.png` shows visible Sonic title/menu UI. The next graphics target is therefore presentation/copy-output correctness, not basic FIFO decode.
- Added `--gx-frame-source <efb|last-display-copy>` so GX dumps can decode the most recent display-copy target from emulated RAM. The 50M Sonic `last-display-copy` capture at `artifacts/sonic-gx-50m-last-display-copy/gx-frame.png` is black, so the visible title/menu content currently appears in pre-clear EFB or texture-copy traffic rather than the final display-copy target.
- Added `--dump-gx-copies <csv-path>` to replay FIFO rendering and log every BP `0x52` EFB copy with draw index, kind, destination, dimensions, clear flags, and source coverage. The first version alpha-gated coverage and hid Sonic's RGB-visible/alpha-zero output; the corrected RGB coverage in `artifacts/sonic-gx-copies-50m-rgb/gx-copies.csv` shows 24 nonblack display copies. The first is copy 5 after draw 4, with 46,741/307,200 RGB-nonblack source pixels and zero alpha-visible pixels, then the copy clear returns EFB to black.
- Added `--dump-gx-coverage <csv-path>` for per-draw EFB RGB coverage deltas and raster-budget accounting. The 50M Sonic coverage in `artifacts/sonic-gx-coverage-50m-rgb/gx-coverage.csv` shows RGB content starts at draw 2, alpha stays zero, and the diagnostic raster budget reaches zero at draw 97. A focused `last-display-copy` dump capped at four draws wrote `artifacts/sonic-gx-first-nonblack-display-copy-50m/gx-frame.png`, confirming the early display-copy target contains the dim Sonic title/menu layer before later black display copies overwrite the latest XFB.
- Coverage diagnostics now honor the existing GX draw window (`--gx-frame-skip-draws` / `--gx-frame-max-draws`), and draw diagnostics print vertex alpha without breaking the old color/texcoord text. An 80M global raster-budget run in `artifacts/sonic-gx-coverage-50m-rgb-80m-raster/gx-coverage.csv` reaches draw 689 before exhausting its budget, proving the previous draw-97 cutoff was diagnostic budget pressure rather than the FIFO going idle.
- Windowed coverage at `skip=680,max=160` in `artifacts/sonic-gx-coverage-50m-window-skip680/gx-coverage.csv` shows the same three-draw dim UI cycle continues past the global budget cliff. A later `skip=1500,max=160` window in `artifacts/sonic-gx-coverage-50m-window-skip1500/gx-coverage.csv` switches to a smaller lower-screen band around draws 1534-1660. The focused display-copy capture at `artifacts/sonic-gx-display-copy-50m-skip1500/gx-frame.png` shows that lower green/blue strip.
- Added `--dump-gx-tev-samples <csv-path>` to sample representative draw triangles through the current TEV evaluator and log raster color, TEV output, alpha-test result, color/alpha update enables, copy count, and per-stage inputs. The command is observational and uses the same draw window as frame dumping, so it can be paired with `--gx-frame-skip-draws` / `--gx-frame-max-draws` without mutating EFB output.
- Early Sonic samples in `artifacts/sonic-gx-tev-samples-50m-early/gx-tev-samples.csv` show the first visible UI layer uses very low vertex/raster alpha. Draw 2 passes through `180/108/40/4`, while the next two textured modulate draws evaluate to `53/0/5/0` and `123/0/64/0` at the sampled centroid and fail alpha test. Alpha update is disabled throughout, matching the earlier alpha-zero display-copy coverage.
- The late lower-band samples in `artifacts/sonic-gx-tev-samples-50m-skip1500/gx-tev-samples.csv` contain 160 sampled draws: 129 `Modulate` draws and 31 alternate pass-color stage-0 cases, which were initially labeled `Unknown` before the classifier learned the `(zero, raster, one, zero)` raster pass-through form. All pass alpha test, all enable color writes, and all disable alpha writes. The nonblack modulate rows repeatedly combine grey vertex fade values with a dark `0/32/8/255` texture sample, producing outputs that ramp up to `0/32/8/255`. This points toward either correct fade/texture-copy content or a texture-address/source-format issue, not a broad TEV color-combiner failure.
- TEV sample diagnostics now choose representative first/middle/last triangles for larger draws and sample four interior barycentric points per selected triangle. The updated early Sonic run in `artifacts/sonic-gx-tev-samples-50m-early-multi/gx-tev-samples.csv` writes 32 rows across the first four draws. It shows the earlier centroid-only transparent reads were incomplete: the same textured quads also hit opaque texture samples such as `115/99/222/255` and `255/255/255/255`, pass alpha, and emit visible TEV output.
- The updated late lower-band run in `artifacts/sonic-gx-tev-samples-50m-skip1500-multi/gx-tev-samples.csv` writes 1,280 rows across 160 draws. It still stays consistently dark across the expanded sample pattern, with texture samples concentrated at `0/32/8/255`, `0/0/16/255`, `0/65/0/255`, and `0/65/8/255`. The 248 rows labeled `Unknown` in that CSV are the same alternate pass-color form and will be labeled `PassColor` on the next capture. The dark green/blue strip is therefore unlikely to be a one-point sampling artifact; the next check should compare the texture-copy source and display-copy path feeding those texels.
- Copy diagnostics now include representative source/readback samples and a `texture_readback_mismatches` count for EFB texture copies. The 50M Sonic run in `artifacts/sonic-gx-copy-readback-50m/gx-copies.csv` logs 3,247 copies: 2,164 texture copies and 1,083 display copies. Every texture-copy readback sample reports zero mismatches, and no texture copy targets the late strip's sampled address `0x00696600`.
- TEV sample summaries now include texture address, format, size, wrap, UV, and texel coordinates. The late strip run in `artifacts/sonic-gx-tev-samples-50m-skip1500-texinfo/gx-tev-samples.csv` shows all textured samples come from a CMPR game asset at `0x00696600`, size `256x32`, not from EFB texture-copy traffic. This narrows the late strip to either correct asset content, CMPR sampling/filter details, or higher-level presentation/scene timing, not EFB copy encoding.
- Fixed CMPR/DXT1 selector handling for the `color0 <= color1` case: selector 3 now decodes as transparent black instead of opaque average color. A post-fix Sonic TEV sample in `artifacts/sonic-gx-tev-samples-50m-skip1500-cmpr-fix/gx-tev-samples.csv` is byte-for-byte equivalent in sampled TEV/alpha results for this window, so the late strip is not caused by that CMPR transparent-selector bug.

- Texture diagnostics now decode mode0 filter and LOD state, and the software sampler honors basic bilinear filtering when the texture's mag filter is linear. A corrected focused Sonic rerun in `artifacts/sonic-gx-tev-samples-50m-skip1500-filter-lod-corrected/gx-tev-samples.csv` shows the late strip texture uses `wrap=repeat:repeat/filter=linear:linear/lod=bias0:min0:max0`, so this asset is not using mip filtering in the sampled window. Linear sampling still changes 512 of 1,280 sampled rows versus the old nearest-only diagnostic, with common samples shifting from `0/0/16/255` to `0/0/14/217` or from `0/32/8/255` to `0/50/8/255`. The strip is still dark blue-green, but the next target is no longer mipmapping for this case; it is broader presentation timing or scene selection.

- Added a focused Sonic decoder dispatch matrix target, `sonic-decoder-dispatch-19m`, after widening indirect-branch profiling to include plain `bcctr` dispatches. The target samples after `0x01200000` instructions and currently records `0x80154DFC -> 0x80158484` with 236 hits, plus the branch guard `0x80154DEC -> 0x80154DF0` with 236 hits. The dispatch arm at `0x80158484..0x80158B8C` is an unrolled coefficient decoder that updates the bitstream state and writes decoded floats through the output table. It is useful to track, but the current 19M window only exposes about 100k instructions of work here, so it is not a high-value fast-forward target compared with the multi-million-instruction resource/GX helpers.

- The 20M matrix benchmark now captures Sonic's Nintendo license screen from the `largest-display-copy` source, with 2,590,648 FIFO bytes and frame hash `037ff703bd85f9b9c35574ffbf2409f0c6738e593c618e1a167ecc924a1c7c10`. Pikmin's 20M benchmark still shows the gray startup/presentation frame, but its CPU milestone has moved to `0x802145E4` while preserving the 29 rendered-quad visual hash. Pikmin's top profile entries are allocator/string/list helpers; the `0x800466C0` heap wait loop is already covered by the idle-cycle fast-forward, which skipped about 101M hardware cycles in this run.

- Added a 35M Sonic matrix milestone. It reaches the dim Sega splash from the `largest-display-copy` source with 12,390,724 FIFO bytes, 369 rendered triangles, XFB address `0x00538460`, copy index 560, and frame hash `7a7ecb70c500c768ab57ce1297ff7b732f9ec97797adeab941b808b7482cc299`. The top PC remains the resource/task wait at `0x800EBEA0` (`lwz r0, -30176(r13); cmpwi 0; beq -8`), followed by the `0x800E3134` resource pump cluster, so the next Sonic CPU target is understanding what drives that resource flag rather than skipping the wait blindly.

- Added `sonic-resource-flag-watch-35m` to track the resource flag behind the `0x800EBEA0` wait. The small-data flag resolves to `0x803ADCE0` for the current Sonic boot, and the focused run stops after 12 late writes at `0x800EC5A0` around 29.48M instructions. The writers toggle bit patterns through `0x800ECA38` (`0x00080080`), `0x800EBE54` (`0x00088080`), `0x800EBF20` (`0x00008080` / `0x00000080`), `0x800EC59C` (`0x00808080`), and `0x800EBB5C` (`0x00008080`). This shows the flag is an active task/resource bitfield during GX work, not a permanently stuck missing interrupt, so the next fix should inspect the producer routines and scheduler/resource semantics instead of skipping the wait.
- Rerunning that target after adding write-watch old/xor diagnostics shows three clean transition bits: `0x800ECA38` sets `0x00080000`, `0x800EBE54` sets `0x00008000`, `0x800EBF20` clears either `0x00080000` or `0x00008000`, and the `0x800EC59C` / `0x800EBB5C` pair toggles `0x00800000`. The low `0x80` bit remains set across the sampled task window.
- Added `--trace-sonic-resource-flags <csv>` and wired it into the matrix trace block for `sonic-resource-flag-watch-35m`. The trace records the late flag producer PCs, operation kind, old/new/xor, computed mask, task pointer, slot, and selected queue links. The gated 35M rerun produces 12 late events: slot 12 (`0x00080000`) and slot 16 (`0x00008000`) are queued/cleared through the selected-task path, while slot 8 (`0x00800000`) is repeatedly activated and cleared by the task path. The broad mask for `clear-task` (`0x04800000`) is expected from the SDK-style `(slot + 1) << (31 - slot)` priority mask, but only the currently set bit changes.
- Added `scripts/summarize-sonic-resource-flags.ps1` and matrix post-processing for `sonic-resource-flags.summary.json`. The `sonic-resource-flag-watch-35m` row now surfaces `sonicResourceFlagEvents=12`, active slots `16,8`, final flag `0x00808080`, and top slot `16` with a longest observed active span of 4,411 instructions. The summary ranks slots by final-active state and active span, and keeps per-slot producer PCs and task pointers so future resource-loop probes can compare scheduler behavior without hand-reading CSV rows.

- Added smarter display-copy frame sources: `last-nonblack-display-copy` and `largest-display-copy`. The renderer snapshots selected display-copy pixels at copy time, so later black copies to the same XFB address no longer overwrite the diagnostic selection. At 50M Sonic, `artifacts/sonic-gx-50m-last-nonblack-display-copy/gx-frame.png` recovers the late nonblack XFB with 52,981 nonblack pixels, and `artifacts/sonic-gx-50m-largest-display-copy/gx-frame.png` picks a slightly fuller variant with 53,655 nonblack pixels. Both are still the lower title/menu strip rather than a complete scene, which means the final black frame was a selection problem, but the emulator has still not reached/presented a full post-prompt scene by 50M.
- Added `sonic-late-black-window` to focus the post-SEGA Sonic FIFO around draw 202. The target disables PNG capture and emits windowed copy, coverage, TEV, and draw diagnostics instead, keeping the expensive replay bounded around the transition where useful display copies previously appeared to turn black.
- Copy diagnostics now honor the same draw window used by frame and coverage diagnostics while still replaying BP state and copy commands outside the window. The first focused run with a 4M raster budget proved the display-copy path was good through draw 210, but then exhausted the diagnostic raster budget and falsely made later rows look black. The corrected 50M-budget run in `artifacts/compat-runs/20260523-130545/sonic-late-black-window` stays nonblack across the whole window: 81 display copies from draws 202-282, all nonblack, with a largest copy of 149,462 pixels at XFB `0x00538460`. The draw log shows repeated full-screen CMPR textured strips fading from low grey to white rather than degenerate geometry. The next Sonic rendering target is therefore a later bounded-window sweep, not XFB selection, YUYV copy encoding, or immediate post-SEGA geometry decode.
- Added generated Sonic GX window targets `sonic-gx-window-280`, `sonic-gx-window-400`, `sonic-gx-window-680`, `sonic-gx-window-1000`, and `sonic-gx-window-1500`, plus coverage rollup columns in `summary.csv`. The first later sweep sample, `artifacts/compat-runs/20260523-132511/sonic-gx-window-680`, shows no display copies within draws 681-760, but coverage becomes nonblack at draw 698 and reaches 37,793 pixels by draw 760 with partial geometry bounds around `-525/-531-397/813`. That brackets the transition: draw 202-282 is still full-screen display-copy fade output, while draw 681-760 is offscreen/partial scene geometry without XFB presentation inside the sampled window.
- The full generated sweep in `artifacts/compat-runs/20260523-134204` tightens that bracket: `sonic-gx-window-280` and `sonic-gx-window-400` both produce 81 nonblack display copies and full-screen bounds `65/-15-576/496`; `sonic-gx-window-680` has no display copies but does have 63 nonblack coverage rows and 62,592 color writes; `sonic-gx-window-1000` and `sonic-gx-window-1500` have no display copies, no color writes, and only offscreen degenerate/out-of-view geometry. The next useful sweep is therefore between draw 480 and 680, followed by a heavy draw/TEV capture on the first no-presentation window.
- The finer sweep in `artifacts/compat-runs/20260523-172950` finds the presentation break: `sonic-gx-window-480` still has 64 nonblack display copies followed by 9 black copies at draws 544-546; `sonic-gx-window-520` still has 24 nonblack display copies followed by the same black tail; `sonic-gx-window-560` has no display copies but still has 77 nonblack coverage rows and peaks at 254,692 EFB pixels. The heavy 560 capture in `artifacts/compat-runs/20260523-181334/sonic-gx-window-560` shows only single-stage `Modulate` TEV with valid CMPR texture samples, while draw bounds/viewing are extreme/offscreen for the first 3D geometry burst. A projection-derived near-clip rerun in `artifacts/compat-runs/20260523-185221/sonic-gx-window-560-heavy` preserves the same 77 nonblack coverage rows and 254,692-pixel peak, so the break is not the near-epsilon clipping path. The next target is the GX presentation timeline and post-fade scene/camera state.
- Added `--dump-gx-copy-events <csv-path>` and the `sonic-copy-events-50m` benchmark target for a low-cost BP `0x52` EFB-copy timeline. Unlike `--dump-gx-copies`, this pass does not rasterize or read back XFB contents. The first run in `artifacts/compat-runs/20260523-190830/sonic-copy-events-50m` writes 1,743 copy events in about 0.6s of post-emulation diagnostics: 585 display copies and 1,158 texture copies. It shows the draw-544..546 black fade is followed by a long gap; the next display copy is at draw 4,424, then display copies alternate XFB addresses every roughly 3,879 draws through draw 120,791. Added `sonic-gx-window-4380` to inspect whether that resumed copy is black or useful.
- `artifacts/compat-runs/20260523-191532/sonic-gx-window-4380` shows the resumed draw-4,424 display copy is barely nonblack: 85 display pixels, with only 19 nonblack source pixels before the copy. The same window's coverage reaches 180,437 nonblack EFB pixels by draw 4,445, after the display copy has already happened, so the next copy slot at draw 8,302 is the better presentation checkpoint. Added `sonic-gx-window-8260` for that follow-up window.
- `artifacts/compat-runs/20260523-192250/sonic-gx-window-8260` repeats the sparse scheduled-copy pattern: display copy at draw 8,302 has 85 nonblack pixels, then the EFB reaches 180,224 nonblack pixels by draw 8,323. This means short windows around copy events are not enough when the interesting EFB content is built after the copy and before the next scheduled display copy. Added `sonic-gx-interval-8302`, which renders the longer interval from draw 8,302 through the next display-copy slot while only emitting copy diagnostics.
- `artifacts/compat-runs/20260523-193027/sonic-gx-interval-8302` confirms the interval approach: the first display copy at draw 8,302 is black in a cold interval, then the next display copy at draw 12,180 has 204,016 nonblack pixels (66.411%) with full-frame bounds. The matching manual frame capture at `artifacts/compat-runs/20260523-193027/sonic-gx-interval-8302-frame/gx-frame.png` shows a recognizable Sonic scene background with a bridge/track structure over a city/night sky. Added `sonic-gx-interval-8302-frame` as the repeatable harness target for this PNG capture.
- Comparing that interval frame against `artifacts/dolphin-reference/20260526-101627/sonic-adventure-2-battle/samples` with `scripts/compare-image-against-samples.ps1` shows no close same-frame match. The Dolphin frame-600/650 samples are the same city/bridge sequence, but ngcSharp's bridge is angled and shifted while Dolphin's bridge remains front-on in the sampled region; frame 600 differs by 270,638 pixels (88.098%). Treat this as a rendering/framing/projection investigation, not merely a copy-selection issue.
- Added `sonic-gx-window-12140-heavy` to capture bounded draw, copy, coverage, and TEV diagnostics immediately before the draw-12,180 full city/bridge display copy.
- Fixed a projected-geometry wrapping bug in the GX diagnostic renderer: the legacy negative direct-screen-space rescue now only applies to unprojected direct coordinates, so XF-projected triangles fully off-screen no longer wrap back into the framebuffer. The rerun in `artifacts/compat-runs/20260526-103914/sonic-gx-interval-8302-frame/auto.png` removes the old erroneous city slab and leaves a cleaner bridge/city frame, but the bridge is still angled relative to Dolphin frame 600. Added `--disable-sonic-paired-transform-fast-forward` plus `scripts/run-retail-benchmarks.ps1 -ExtraRunArgs`; the A/B run in `artifacts/compat-runs/20260526-105237` is pixel-identical to the normal run, so the remaining bridge angle mismatch is not caused by the Sonic paired-transform fast paths.
- Added `--dump-gx-transforms` and `scripts/summarize-gx-transforms.ps1`. The `sonic-gx-window-12140-heavy` run in `artifacts/compat-runs/20260526-115628` wrote 80 transform rows: all are perspective, 66 project at least one vertex, 28 clip at least one vertex, and all 80 have identity-like model/view bounds. That means the city/bridge vertices in this window are already emitted in view space; the remaining angle mismatch is more likely upstream scene/camera/timing than a software projection bug in this diagnostic renderer. Added `sonic-gx-window-12140-heavy-no-held-input` and `sonic-gx-interval-8302-frame-no-held-input` to compare the same windows without holding A from boot. The no-held-input frame run in `artifacts/compat-runs/20260526-121325` is pixel-identical to the held-A baseline (`0` changed pixels), so the held-A input shortcut is not the cause of the bridge angle. Comparing that no-held-input frame against the fine Dolphin sample set in `artifacts/image-sample-compare/20260526-122211` still finds no close match; the best changed-pixel score is 88.211% against requested frame 550.
- Added `--disable-sonic-gx-fast-forward` and `--disable-sonic-geometry-fast-forward` to isolate compatibility risk from Sonic-specific shortcuts. The GX-disabled run in `artifacts/compat-runs/20260526-123826` and the geometry-disabled run in `artifacts/compat-runs/20260526-124739` are both pixel-identical to the normal city/bridge frame, with the intended counters at zero, so the mismatch is not caused by those fast-forward helpers.
- Added bounded FIFO writer tracing with `--trace-gx-fifo-window <csv> <start> <len>`. The bridge-window trace in `artifacts/compat-runs/20260526-gx-fifo-window/gx-fifo-window.csv` captured 11,567 writes around FIFO `0x2CEF00..0x2D7EFF` while Sonic GX fast-forwards were disabled. The dominant writer PCs are the real FIFO helper stores at `0x80120148..0x80120150` and `0x8012012C..0x8012013C`, plus the earlier strip helper at `0x8011D8E8..0x8011D914`. The next investigation should profile the callers feeding `0x80120158`/`0x8011D91C` rather than the renderer or shortcut emitters.
- LR profiling climbed the Sonic draw path from FIFO leaf stores to the command-stream renderer. The store helpers return to tight loops at `0x80120078..0x80120108` and `0x8011D830..0x8011D8C4`; the real prologues are `0x8011FFF4` and `0x8011D7B4`. Those are reached from command-stream interpreter `0x8011CCF8`, which is called mostly from `0x8011D4D8` (556 hits, 81.287%) and secondarily from `0x8011D53C` (128 hits, 18.713%) in `artifacts/compat-runs/20260526-gx-object-dispatch-lr-profile-30m`. PC traces at `0x8011D414` show the active draw packet pointers in `r3` around `0x813184D0..0x8132A68C`, the global vertex base through `r13-29028`/`r4=0x80B28500`, and packet-type variants in `r30` (`0x14`/`0x16`). The next narrow probe should snapshot those draw packet structures and the vertex-base table near the mismatched bridge packets, then compare their decoded command/vertex data against Dolphin or a same-scene reference.
- Added `--trace-sonic-draw-packets <csv>` for the `0x8011D414` packet renderer entry, plus `scripts/summarize-sonic-draw-packets.ps1` for offline grouping/decoding. The first capped run, `artifacts/compat-runs/20260526-sonic-draw-packets-30m/sonic-draw-packets.csv`, captures 80 packet snapshots after 18M instructions and now has a compact `sonic-draw-packets.summary.json`. The packets are stable 0x28-ish structures: word 0 is stream0, word 1 is stream1, words 2/3/4/5 look like transform or bounds floats, word 6 is the packet kind (`0x14` or `0x16`), and word 7 points back to the packet. All snapshots use vertex base `0x80B28500`. The summary shows two dominant families: 36 kind-`0x14` packets with `r4=0`, `r6=0x802D7758`, `r7=0`, and 29 kind-`0x16` packets with `r4=0x803AE0C4`, `r6=0x10`, `r7=1`. Stream0 starts with big-endian halfwords where `h0 == h2 * 4 + 1` in the dominant groups, so `h2` is a strong candidate payload word count; the top stream group has count 90, and other repeated groups have counts 15, 40, 54, 120, 128, and 172. Stream1 consistently begins with opcode/subcode `0x25/0x13`. Future traces now widen stream0 capture from the candidate byte length, capped at 0x800 bytes, and stream1 to 0x200 bytes. The summarizer can also emit `sonic-stream0-records.csv`, a packet/index/word/float expansion of stream0 payload words, to make differential comparisons against GX FIFO emission easier. The next narrow probe is decoding those two streams into vertex/index/color records and matching them to the bridge draw packets.
- Added `scripts/summarize-gx-fifo-window.ps1` and `--trace-sonic-gx-emitters <csv>` to close the gap between packet streams and FIFO writes. The bridge FIFO window in `artifacts/compat-runs/20260526-gx-fifo-window` now has `gx-fifo-window.summary.json` and `gx-fifo-window.vertices.csv`; it identifies 977 candidate vertices from the dominant `0x80120148..0x80120150` float emitter. A focused real-emitter capture in `artifacts/compat-runs/20260526-sonic-gx-emitters-80120148/sonic-gx-emitters.csv` confirms the FPR single bits at `0x80120148` exactly match the FIFO vertex words, with `r29` becoming the first halfword attribute (`0x0608`, `0x058E`, `0x0400`, etc.) and `r30` carrying a small loop/strip counter. Nontrivial direct word overlap with packet stream0 records is zero, so the remaining bridge mismatch is upstream transform/decode state rather than the FIFO store helper corrupting values.
- Extended the Sonic GX emitter trace to include the source load sites at `0x801200C8..0x801200D4`. The focused run in `artifacts/compat-runs/20260526-sonic-gx-emitter-source-80120148/sonic-gx-emitters.csv` shows `r25=0x80B28500` as the vertex-base table, `r27` as `r25 + index * 0x20`, and source words at `r27+0/4/8/0x18` already matching the emitted FIFO position/color words. The first bridge-window source records are `0x80B286E0`, `0x80B287C0`, `0x80B28520`, `0x80B28A60`, etc., while `r24` walks through stream data at `0x81317692`, `0x81317698`, `0x8131769E`, etc. That narrows the bridge angle mismatch to the producer of the vertex-base table or the stream index/attribute decode feeding this loop, not the load/FIFO helper itself.
- The emitter trace now records `gx_fifo_offset`, which lets paired-transform fast-forward A/B runs align by FIFO position instead of executed instruction count. The no-paired run in `artifacts/compat-runs/20260526-sonic-gx-emitter-source-fifo-offset-no-paired-ff-stop-2d1e42` stops exactly on FIFO offset `0x2D1E42` and matches the normal run's first bridge vertex (`source_record=0x80B286E0`, `f1/f2/f3=0x4420EB31/0x45208C27/0xC2A2CEA0`, `r29=0x0608`). This clears the paired-transform fast-forward for the first bridge vertex; the next investigation should decode the stream cursor/index records that select source records and attributes.
- Added `scripts/summarize-sonic-emitter-stream.ps1` to decode the stream cursor records observed by the emitter trace. For the focused bridge window, `artifacts/compat-runs/20260526-sonic-gx-emitter-source-fifo-offset/sonic-emitter-stream-records.csv` maps all eight sampled source rows back into packet `0x813184D0` stream1 with zero index/attribute mismatches. The record shape is three signed big-endian halfwords at `stream_cursor - 6`: `index`, `r29`, and `r28`. Example: record bytes `000F0608FDD8` at `0x8131768C` decode to index `15`, attr0 `0x0608`, attr1 `0xFDD8`, which selects source record `0x80B286E0` from vertex base `0x80B28500`. That pins the mismatch upstream to packet `0x813184D0` contents or the scene/camera data that generated its vertex table and stream records.
- Added `--trace-sonic-transform-inputs <csv>` for the Sonic paired-single transform loops at `0x8011DAA8`, `0x8011DB94`, and the indexed 4D variant. It captures input/output cursors, GQRs, FPR pairs, and compact memory windows before each loop entry. The initial normal run in `artifacts/compat-runs/20260526-sonic-transform-inputs-vertex-table` confirms the 2D transform batches all write to the shared `0x80B284F8` output table with `GQR1=0x08040804`, while the no-paired-transform run in `artifacts/compat-runs/20260526-sonic-transform-inputs-wide-no-paired-ff` shows the real 4D loop walking 0x20-byte output records under `0x80B28500`. Because disabling the transform shortcut shifts instruction phase, future transform A/B probes should align by output address or FIFO offset instead of raw instruction count.
- Added `--trace-sonic-transform-output-range <addr> <len>` and `scripts/summarize-sonic-transform-inputs.ps1` so transform probes can target a specific output-table record. The focused bridge probe in `artifacts/compat-runs/20260526-sonic-transform-output-80b286e0-no-paired-ff-v2` reaches `0x80B286E0` through loop rows at `output_cursor=0x80B286D8` and `0x80B286F8`; the first row maps to packed input `0x81317EB8`, which is the expected `15 * 0x10` record offset inside the packet `0x813184D0` stream. This ties the earlier emitter source record `0x80B286E0` back to its packed transform input and keeps the current Sonic bridge mismatch focused upstream of GX rasterization.
- Added `scripts/compare-sonic-transform-traces.ps1` to align fast-forward transform batches with no-fast-forward per-record rows by output address. Comparing `artifacts/compat-runs/20260526-sonic-transform-output-80b286e0/sonic-transform-inputs.csv` against `artifacts/compat-runs/20260526-sonic-transform-output-80b286e0-no-paired-ff-v2/sonic-transform-inputs.csv` at `0x80B286E0` produces `sonic-transform-ab-compare.json` with zero stable-field mismatches for the rows captured in both runs. The only unmatched rows are later fast-forward batches beyond the no-fast-forward trace limit, so the current evidence clears the Sonic paired-transform fast-forward as the source of the bridge angle mismatch.
- Added `--trace-sonic-input-writes <csv> <addr> <len>` and `--disable-sonic-bit-unpack-fast-forward` for packed model input provenance checks. The bridge input range `0x81317EB8..0x81317ED8` is written by the bitstream/model decoder around `0x800D2684`, and the focused late capture in `artifacts/compat-runs/20260527-sonic-input-writes-81317eb8-late` finishes with `FF0000004537DD11C50E0064C537DD12FFFFFFFF454F1820C499B38BC54F1821`, matching the transform input snapshot. A companion run with Sonic bit-unpack fast-forward disabled, `artifacts/compat-runs/20260527-sonic-input-writes-81317eb8-no-bitunpack-ff`, produces the same final 0x20 bytes with `bitUnpackInstructions=0`, so this specific bridge record is not being corrupted by that shortcut.
- Added `scripts/build-sonic-bridge-timeline.ps1` to stitch packet, stream-index, transform, input-write, source-record, and FIFO-emitter evidence into one CSV. The bridge timeline in `artifacts/compat-runs/20260528-sonic-bridge-timeline` now has a single-record view for `0x80B286E0` and an eight-row packet view from the wider transform capture `artifacts/compat-runs/20260528-sonic-transform-output-bridge-band-no-paired-ff`. All eight sampled vertices from packet `0x813184D0` share the same transform matrix/base pairs (`f0..f7`), so the bridge angle mismatch is now narrowed past stream decode and per-vertex transform variation; the next probe should compare this packet's shared matrix/camera state against a visually correct nearby packet or a Dolphin-aligned reference frame.
- Added `scripts/summarize-sonic-packet-matrices.ps1` and a wider real-emitter capture in `artifacts/compat-runs/20260528-sonic-gx-emitter-neighbor-window-v4` with Sonic GX fast-forward disabled. The decoded neighbor timeline contains 120 source rows: 72 rows from packet `0x813184D0` with the bridge matrix, 45 later rows whose packet stream fell outside the captured draw-packet window but still use the same `f0..f7`, and 3 rows from the same packet with `GQR1=0x08040804` and different base pairs. This shows the main bridge packet is internally coherent across a larger FIFO span; the next useful comparison needs either a visually good packet from a different draw-packet window or a Dolphin-aligned camera/matrix reference, because immediate neighbors largely share the same transform state.
- Added `scripts/run-sonic-visual-anchor.ps1` to make the city/bridge mismatch repeatable as a visual regression artifact. It can either run the known draw-8,302/3,900-draw Sonic capture or recompare an existing PNG via `-CandidatePath`, then writes full-frame, skyline, bridge, and lower-track comparisons against the latest Dolphin Sonic samples plus a contact sheet. Reusing `artifacts/compat-runs/20260526-103914/sonic-gx-interval-8302-frame/auto.png` produced `artifacts/sonic-visual-anchor/20260529-020136`: the full-frame best match is still requested frame 550 at 88.211% changed, while the bridge and lower-track crops best match requested frame 600 but differ by 99.777% and 99.937%. That gives the next rendering fixes a focused crop metric instead of only a broad whole-frame score.
- Added `--dump-gx-vertices <csv-path>` for machine-readable per-vertex GX diagnostics in the same draw window used by draw, TEV, texture, and transform dumps. A gx-ladder smoke run at `artifacts/gx-vertex-smoke/gx-vertices.csv` confirms the CSV includes draw/FIFO offsets, vertex index, raw bytes, model/view/screen coordinates, clip state, color, and TEX0 values. A full Sonic bridge extraction still needs a longer run budget than the quick 4-minute check, but the tool is ready for the next bridge packet pass.
- Ran the full Sonic bridge vertex extraction in `artifacts/compat-runs/20260529-023815/sonic-bridge-vertices` and added `scripts/summarize-gx-vertices.ps1` for per-draw bounds plus focused FIFO windows. The exact first bridge payload offset `+0x2D1E42` now appears as draw `12181`, vertex `0`, with raw bytes beginning `44 20 EB 31 45 20 8C 27 C2 A2 CE A0`, matching the earlier emitter trace. Its view-space position is already `643.675/2568.76/-81.404` and projects to `6450.916/-24227.09`, while adjacent draws around `+0x2D1E3F..+0x2D20AC` have very large offscreen bounds and multiple clipped vertices. This strongly points upstream of final raster/copy selection: the bridge packet's emitted coordinates/camera state are already wrong or wrong-timed before diagnostic rasterization.
- The bridge transform loop loads its shared matrix/source values from locked-cache address `0xE0000090` (`r4` at `0x8011DAA8`), so ordinary main-RAM write watches cannot see the producer. Added a `GameCubeBus.LockedCacheWriteObserver` and CLI diagnostic `--trace-locked-cache-writes <csv> <addr> <len>` to trace those stores with PC/opcode/register context plus watched-range bytes. The matrix destination trace in `artifacts/compat-runs/20260529-sonic-locked-cache-matrix-writes` shows `0x8011C164..0x8011C184` copying paired-single values into `0xE0000090..0xE00000BC`, with `r4=0x803AE0C4`, `r5=0xE0000060`, and `r6=0xE0000090`. The source trace in `artifacts/compat-runs/20260529-sonic-locked-cache-source-writes` shows `0x801160B8..0x801160DC` first copying the prior stack slot into `0xE0000060..0xE000008C`, then `0x801161BC..0x801161C4` mutating translation components. The previous-slot trace in `artifacts/compat-runs/20260529-sonic-locked-cache-prev-slot-writes` reaches `0x8010C07C..0x8010C0C4`, which copies lower stack data into `0xE0000030..0xE000005C`. This pins the bad bridge camera/matrix state to Sonic's locked-cache matrix-stack producer chain rather than GX FIFO emission, transform fast-forwards, or final raster/copy selection.
- A main-RAM watch on the current matrix pointer `0x803AE0C4` in `artifacts/compat-runs/20260529-sonic-matrix-global-writes` shows the late bridge sequence explicitly: `0x801124D0` initializes the current pointer to `0xE0000000`, `0x80115DAC` advances it to `0xE0000030`, `0x801160B0` advances/copies to `0xE0000060`, `0x8011C19C` advances/transposes to `0xE0000090`, and `0x80116100` pops it back to `0xE0000060`. LR profiling in `artifacts/compat-runs/20260529-sonic-matrix-stack-lr-profile` ties the `0xE0000090` push/transpose to the draw-packet renderer around `0x8011D430`, the pop to `0x8011D450`, and the preceding object transform push to `0x8011E068`. Added `--trace-sonic-matrix-stack <csv>` so the next bridge run can capture those calls, object flags, packet pointers, and current matrix bytes in one artifact.
- The first matrix-stack trace, `artifacts/compat-runs/20260529-sonic-matrix-stack-trace/sonic-matrix-stack.csv`, captures the bridge packet object directly. At `0x8011E064`, `r27=0x813184E8` and the object bytes start `00000016 813184D0 C244FDA8 C3F394F7 ...`, linking kind `0x16` object `0x813184E8` to draw packet `0x813184D0`. The transform vector at `r3=0x813184F0` is `-49.2477/-487.164/0`, followed by identity scale-like fields, then `0x801161BC..0x801161C4` rewrites the translation components in the current `0xE0000060` matrix to `C424FD8A/C4185E2E/C4392CB5`. The draw-packet renderer then transposes that matrix into `0xE0000090` at `0x8011C19C`. The next compatibility probe should compare this object transform against Dolphin or against a visually aligned packet; the matrix stack machinery itself now looks internally coherent.
- Added `scripts/summarize-sonic-matrix-stack.ps1` to decode those traces into object kind, packet pointer, object transform fields, matrix rows, and translation vectors. The wider run in `artifacts/compat-runs/20260529-sonic-matrix-stack-wide` captures three adjacent render objects: kind `0x16` object `0x813184E8 -> packet 0x813184D0` translates to `-659.962/-609.472/-740.699`, while nearby kind `0x14` objects `0x81318FE0 -> 0x81318FC8` and `0x81319AD8 -> 0x81319AC0` share the same rotation rows but translate to `131.77/-554.098/2797.493` and `46.938/-594.727/2606.718`. This suggests the bridge packet is not suffering a one-off matrix-stack copy bug; it is one object in a coherent object batch whose object-space positions/camera phase need comparison against a reference.
- Widened `--trace-sonic-matrix-stack` with previous/base matrix-slot captures plus `r4/r5/r6` context, and taught `scripts/summarize-sonic-matrix-stack.ps1` to emit previous/base translations. The parent-aware run in `artifacts/compat-runs/20260529-sonic-matrix-parent-wide` shows the bridge packet and both neighbor packets all inheriting the same previous translation `-229.246/-130.726/-831.528` before object-specific translation is applied. At draw submission, the bridge reaches `-659.962/-609.472/-740.699`, neighbor `0x81318FC8` reaches `131.77/-554.098/2797.493`, and neighbor `0x81319AC0` reaches `46.938/-594.727/2606.718`. This clears the local matrix stack push/copy path; the active Sonic visual mismatch is now most likely in transformed vertex emission, camera/projection state, FIFO interpretation, or rasterization timing.
- Added `--trace-sonic-matrix-writer <csv>` plus `scripts/summarize-sonic-matrix-writer.ps1` to capture and summarize the exact paired-single store sequence that feeds Sonic's transform matrix. The bridge run in `artifacts/compat-runs/20260530-sonic-bridge-matrix-writer` captures packet `0x813184D0` at `26653655..26653663`: LR `0x8011D430`, `r3/r30=0x813184D0`, streams `0x81317DA4/0x81317674`, source translation `-659.618/-609.312/-742.082`, and packed transform translation with the same values. Neighbor terminal rows for `0x81318FC8` and `0x81319AC0` also carry coherent packet pointers and translations. This tightens the boundary again: the handoff from object matrix to transform input is behaving consistently, so the remaining bridge mismatch is likely a higher-level resource/camera phase issue or a screen/reference alignment issue, not a bad paired-single store, locked-cache write, FIFO stream decode, or PRS/RVZ data corruption.
- Added `--trace-sonic-scene-state <csv>` and `scripts/summarize-sonic-scene-state.ps1` for packet-entry phase snapshots. The bridge run in `artifacts/compat-runs/20260530-sonic-bridge-scene-state` captures 15 draw-packet rows after `26.65M` instructions. The bridge packet `0x813184D0` and nearby packets `0x81318FC8`/`0x81319AC0` share the same resource flag `0x00000080`, state bytes `0/0`, state word `0x00000008`, zeroed mode globals, vertex base `0x80B28500`, and matrix stack phase `current=0xE0000060 previous=0xE0000030`; the full `0x801CC168` state hash is identical across all 15 rows. Their object vectors differ exactly as the decoded resource predicted (`-49.2477/-487.164/0`, `0/-1170/-3549`, `0/-1170/-3349`). That makes a broad mode/resource phase mismatch unlikely in this window; the next useful target is the packet/object selection path or a Dolphin-aligned same-packet reference, not another low-level transform/FIFO trace.
- Added `--dump-gx-state-timeline <csv-path>` to emit a bounded draw-window timeline of relevant CP/XF/BP/copy/draw state with active viewport/projection, position matrix rows, scissor, pixel mode, and EFB copy fields. The Sonic bridge run in `artifacts/compat-runs/20260529-sonic-gx-state-timeline-bridge` writes 310 events around draws `12171..12240`. For bridge draws `12181..12196`, GX uses position matrix base `0x0000` with identity rows, perspective projection, viewport scale `320/-240`, viewport origin `662/582`, projection values `2.423/3.231/-1`, and scissor `0..639/0..479`. This makes the GX XF/projection path look ordinary; the extreme bridge coordinates are already present in the FIFO vertex payload/model-view data before diagnostic projection.
- Added `scripts/build-sonic-object-visual-map.ps1` to join matrix-stack summaries with GX vertex bounds using explicit packet-to-FIFO anchors. Anchoring `0x813184D0=0x2D1E3F` maps the bridge object to draws `12181..12196`: 144 decoded vertices, 85 clipped vertices, view bounds `-6753.44/-3019.876/-5225.021 .. 5810.573/2766.324/4028.155`, and screen bounds `-373897.4/-78733.38 .. 21380.3/2283.011`. Anchoring the next packet, `0x81318FC8=0x2D375C`, maps the nearby kind-`0x14` object to draws `12212..12220`: all 70 decoded vertices are clipped, with view bounds around `106..192/-520..-459/2834..2857`. The bridge object is therefore the mapped source of the extreme on-screen/off-screen bridge cluster, while the next object sits fully clipped in this capture.
- Added `scripts/build-sonic-packet-timeline.ps1` to join scene-state summaries, matrix-writer terminal rows, and optional anchored GX vertex bounds. Running it over `artifacts/compat-runs/20260530-sonic-bridge-scene-state` with anchors `0x813184D0=0x2D1E3F` and `0x81318FC8=0x2D375C` writes `sonic-packet-timeline.csv`: packet-to-matrix latency is 30 instructions for the bridge and every mapped neighbor, the bridge maps to draws `12181..12196` with 144 decoded / 85 clipped vertices and extreme screen bounds `-373897.4/-78733.38 .. 21380.3/2283.011`, while the next packet maps to draws `12212..12220` with all 70 vertices clipped. This makes the bridge packet itself the first problematic visible packet in the window; the next target is deriving more packet-to-FIFO anchors automatically or tracing the packet-list/object selection caller before `0x8011D414`.
- Added `--trace-sonic-packet-selection <csv>` and `scripts/summarize-sonic-packet-selection.ps1` to capture the wrapper/dispatch path before Sonic's packet renderer. The focused run in `artifacts/compat-runs/20260530-sonic-packet-selection-v2` resolves callback rows from object pointers and wrapper rows from packet pointers: bridge object `0x813184E8 -> packet 0x813184D0` flows through `callback_wrapper_entry`, `callback_dispatch_call`, `primary_wrapper_entry`, `primary_wrapper_call`, then `renderer_entry`, exactly like neighbors `0x81318FE0 -> 0x81318FC8` and `0x81319AD8 -> 0x81319AC0`. A broad `scripts/summarize-sonic-decoded-resource.ps1 -ScanAllPackets -PacketOnly` pass in `artifacts/compat-runs/20260530-sonic-depth-field-scan` shows packet word `+0x14` is best treated as a bounding radius: bridge packet `0x813184D0` is the largest packet in the decoded resource at `4886.078`, while the two neighbors are small repeated packets at `89.67852`. The next narrow probe should trace the culling helper at `0x8012192C`, which consumes packet bounds before wrapper dispatch, rather than treating the bridge radius as a malformed sort key.
- Extended packet-selection tracing with the bounds-culling helper PCs around `0x8012192C`. The focused bridge/neighbor run in `artifacts/compat-runs/20260530-sonic-cull-packet-selection` captures wrapper and renderer rows for `0x813184D0` and `0x81318FC8`, but no cull-helper rows, confirming the wrapper does not invoke the bounds culler in this phase. Combined with the resource scan, this clears packet `+0x14` as an active bad sort/cull value; the bridge issue remains in same-scene visual alignment, camera/reference timing, or transformed vertex data rather than dispatch/culling.
- Added `scripts/summarize-sonic-input-writes.ps1` and traced writes to `0x813184D0..0x813185CF` in `artifacts/compat-runs/20260529-sonic-bridge-object-writes`. The watched packet/object range is produced by Sonic's bitstream/LZ-style decoder at `0x800D2684`, not by a later live object-update routine. Two decode batches touch the range: an earlier batch at instruction `15070946` from source `0x81593E60`, then the final contents at instruction `15402181` from source `0x80FFFE60`. Follow-up byte-exact capture with `disc-read-bin`, `--dump-memory-bin`, and `scripts/compare-binary-files.ps1` showed the bridge decode source buffer `0x80FFFE60..0x81058F1F` matches disc offset `0x1FB09CA0` for `0x590C0` bytes with zero mismatches. This clears RVZ extraction and DI DMA corruption for the bridge source buffer; the bridge-object investigation now sits at the decoder/resource interpretation boundary.
- Added `--trace-sonic-bitstream-decoder <csv> <addr> <len>` to turn Sonic's PRS-style decompressor fast-forward into a structured diagnostic. The trace records only decompressor calls whose output overlaps the requested range, including compressed source bytes, destination, source end, decoded length, overlapped target bytes, and final flag-bit state.
- The first bridge decoder trace in `artifacts/compat-runs/20260529-sonic-bitstream-decoder-bridge/sonic-bitstream-decoder.csv` captured two overlapping decompressor calls. The final bridge packet is produced at instruction `15402181` from source `0x80FFFE60..0x810A2681` into destination `0x8125FE60`, decoded length `0x108DA5`, with the bridge packet slice at output offset `0xB8670`. The target output bytes begin `81317DA481317674397FDA40431A4168B97FDA40...`, matching the packet/object write trace. The next step is a standalone PRS replay test from this source buffer and output offset so resource decode can be validated without a full Sonic boot.
- Refactored the Sega PRS decoder into `SegaPrsDecoder` and added the `prs-decode` CLI command. A wider source capture in `artifacts/compat-runs/20260529-sonic-prs-replay-bridge/source-80fffe60-wide.bin` replays outside the emulator to decoded length `0x108DA5`, consuming `0xA2821` source bytes, and its `0xB8670+0x100` slice matches the trace's bridge packet bytes with zero mismatches. The decompressor implementation and fast-forward output are now validated independently; the next target is interpreting the decoded resource layout and relocation/pointer semantics around that slice.
- Added `scripts/summarize-sonic-decoded-resource.ps1` for PRS replay artifacts. The first pass over `decoded-80fffe60.bin` treats the output as loaded at `0x8125FE60` and emits address windows, packet summaries, stream0 words, stream1 signed-halfword triplets, and a whole-resource pointer scan. For the bridge window, packet `0x813184D0` has stream pointers `0x81317DA4/0x81317674`, object `0x813184E8`, kind `0x16`, and object vector `-49.2477/-487.164/0`. Neighbor packets `0x81318FC8` and `0x81319AC0` follow the same packet/object self-pointer pattern with kind `0x14` and different stream pointers. The stream1 triplet output reproduces the known bridge emitter record at `0x8131768C`: index `15`, attr0 `0x0608`, attr1 `0xFDD8`.
- Runtime-vs-replay comparison in `artifacts/compat-runs/20260529-sonic-runtime-vs-prs-replay` dumped `0x8125FE60+0x108DA5` immediately after decompression and compared it to `decoded-80fffe60.bin`; all `1,084,837` bytes match. A post-decode bridge trace through `17,070,000` instructions recorded zero writes to `0x813184D0..0x813185CF`. A wider resource trace did find fixups outside the bridge packet, mainly PC `0x80BC6234` writing pointer-like entries around `0x813648A8..0x81364F1F` and PC `0x80BC5ED8` touching the resource header at `0x8125FE64`. This clears the bridge packet contents from post-decode mutation; the remaining issue is how its valid packet/object data is interpreted downstream.
- The packet-family summary now emits stream lengths and object vectors. The bridge kind-`0x16` packet has `stream1_length=0x730` (306 triplets) and `stream0_length=0x72C` (459 words), while the neighboring kind-`0x14` packets each have `stream1_length=0x500` (213 triplets) and `stream0_length=0x5AC` (363 words). Their object vectors differ sharply: bridge `-49.2477/-487.164/0`, neighbors `0/-1170/-3549` and `0/-1170/-3349`. The resource data is coherent and self-referential, not obviously malformed.
- Added `--dump-pointer-table <addr> <count> <stride> <ptr-offset> <target-words>` for compact resource/list diagnostics. Sonic's `0x80116B9C` tail is a 0x500-entry, 0x18-byte resource table lookup loaded from `r13 - 29196/-29192`; at the first late stop around 45.5M it searched for key `0x0089549A` before that object was registered, while the 50M table contains 41 active resources including `0x0089549A..0x008954A9`. A 50M-era stop now searches for `0x007A1201`, and the PC profile is dominated by the variable bitstream/decompression routine at `0x800D2684..0x800D2824`, so the next CPU-side target is understanding resource decode/progression rather than treating the table scan as an idle wait.
- Fixed a paired-single merge aliasing bug in the PPC interpreter: `ps_merge00/01/10/11` now snapshots both source lanes before writing the destination FPR. The Sonic root-matrix probe showed the old `ps_merge00 f4,f2,f4` path turning an identity Z rotation row into `1/1/0/0`; after the fix, `artifacts/compat-runs/20260531-sonic-root-rotation-merge-fix` reports orthonormal root rows and the bridge handoff in `artifacts/compat-runs/20260531-sonic-bridge-after-psmerge-fix` now has orthonormal source rows with translation near `-49.998/-609.046/-744.443` instead of the earlier skewed `x+y` matrix. Visual anchor deltas improved in `artifacts/sonic-visual-alignment/20260531-164542` (bridge crop average delta `13.174`, skyline `7.163`, lower-track `20.610`), but the remaining scene still does not line up with Dolphin, so the next target remains packet/resource/camera phase rather than the root rotation builder.
- Added `sonic-gx-window-12140-provenance` to the retail benchmark harness. It reruns the bridge window with bounded GX state timeline, per-vertex CSV output, and Sonic packet-to-FIFO provenance across FIFO `0x2D1E00..0x2D5DFF`, then auto-summarizes vertices around FIFO offset `+0x2D1E42` plus packet anchors from `sonic-vertex-provenance.csv`; use it as the standard post-fix bridge provenance capture instead of assembling those diagnostics by hand.
- The post-fix provenance run in `artifacts/compat-runs/20260531-202500/sonic-gx-window-12140-provenance` writes anchors for three packets: `0x813184D0=0x2D1E42` (286 rows), `0x81318FC8=0x2D375F` (187 rows), and `0x81319AC0=0x2D4B2D` (187 rows), all with zero stream index/attribute mismatches. Joining those anchors to GX vertices maps the bridge packet to draws `12181..12192`, with 104 decoded vertices, 54 clipped vertices, view bounds `-4721.726/-2641.53/-5225.021 .. 4046.956/2766.324/4028.155`, and screen bounds `-452870.7/-78733.38 .. 9256.052/128.423`. This confirms the bridge packet remains the first problematic visible packet after the CPU merge fix; the next useful trace is a same-run scene/matrix-state capture or a Dolphin-aligned packet/camera reference, not more FIFO helper validation.
- `sonic-gx-window-12140-provenance` now captures scene-state, matrix-writer, and packet-selection traces in the same run, then summarizes and joins them automatically. The same-run artifact `artifacts/compat-runs/20260531-214848/sonic-gx-window-12140-provenance` captured 1,388 scene rows, 2,000 matrix-writer rows, 2,000 packet-selection rows, and a rebuilt `sonic-packet-timeline.csv`. For bridge packet `0x813184D0`, the same-run matrix translation is `-50.306/-609.312/-742.082`, object vector is `-49.2477/-487.164/0`, and the joined GX span is still draws `12181..12192` with 104 decoded / 54 clipped vertices and extreme projected bounds. The CPU-side matrix handoff and packet/FIFO provenance now agree in one artifact; the remaining mismatch is likely either source geometry/camera phase versus Dolphin or a diagnostic renderer clipping/projection edge case around this large near-camera packet.
- Added transform-input capture and per-packet affine fitting to the same provenance bundle. The run in `artifacts/compat-runs/20260531-223828/sonic-gx-window-12140-provenance` maps 660 provenance rows back to Sonic's packed transform inputs and fits each captured packet separately. Packet `0x813184D0` fits 273 points with average residual `0.000169`, row lengths `1.000000086/1.000000096/1.00000002`, and dot products near zero; packets `0x81318FC8` and `0x81319AC0` fit similarly. This rules out a corrupt transformed vertex-table producer for the captured bridge span: the source geometry is being transformed by a clean orthonormal matrix, so the next comparison should be against Dolphin camera/resource timing or the diagnostic renderer's handling of large near-camera geometry.
- Added `--dump-memory-bin-at <instruction> <addr> <len> <path>` so short-lived decode/copy buffers can be captured before later reuse. The Sonic dark bridge texture at `0x0072C600` is bound by `0x801037A4`, sampled by single-stage modulate TEV at mip 0, and copied into texture RAM by the `0x8010C128` copy loop from temporary buffers `0x8137DF9C`, `0x8137EAAC`, and `0x8137EDBC`. Timed snapshots in `artifacts/compat-runs/20260601-sonic-bridge-texture-source-write-provenance-15346k` match the final destination byte-for-byte when compared from source `+4` to destination offsets `0x000`, `0xB20`, and `0xE40`. A source-write trace shows the payload is produced earlier by Sonic's bitstream decoder at `0x800D2684`, with decoded output `0x8125FE60+0x11E13C`. This clears TEV setup, texture binding, RAM copy, and end-of-run buffer reuse as causes of the dark bridge material; remaining investigation should compare the decoded texture asset/content or scene timing against Dolphin.
- Added `scripts/build-sonic-decoded-texture-report.ps1` and `scripts/summarize-sonic-gvrt-textures.ps1`. The decoded texture report for `0x0072C600` in `artifacts/compat-runs/20260601-sonic-bridge-texture-source-write-provenance-15346k/decoded-texture-report` decodes the renderer-consumed CMPR mip chain and matches the renderer's dumped mip0 PNG with zero pixel differences. The corrected mip chain consumes `0xB00` bytes, with mip0 luma `3.281` and every mip remaining extremely dark. A fresh PRS replay from disc offset `0x1FD19C24` decoded `0x127AC0` bytes to `artifacts/compat-runs/20260601-sonic-bridge-texture-decoded-resource/decoded-8125fe60.bin`; the slice at `0x11E13C` matches the timed source and final destination chain byte-for-byte. The GVRT scanner finds the owning header at `0x8137DF90` (`CMPR`, `64x64`, `0xB00` bytes) plus neighboring texture resources at `0x8137A970`, `0x8137B480`, `0x8137EAA0`, `0x8137EDB0`, and `0x8137F8C0`. This makes the dark bridge material look like a normal decoded texture resource rather than corrupted renderer state; next compare these neighboring resources/material assignments against Dolphin or a correct scene phase.
- Added `scripts/build-sonic-gvrt-texture-contact-sheet.ps1` to decode nearby `GVRT` mip0 images, compute raw texture hashes, and join them against runtime GX texture dumps/material rows. The contact sheet in `artifacts/compat-runs/20260601-sonic-bridge-texture-decoded-resource/gvrt-contact-sheet` shows six neighboring CMPR resources around `0x8137DFA0`; several are bright UI/structure textures, but only the dark `0x8137DFA0` resource hashes into the captured runtime draw window. It maps to runtime texture `0x0072C600` across draws `12181..12188`, and the joined material row for draw `12188` has `76,183` color writes with black-write ratio `0.596157`. This pushes the dark bridge question past neighboring-resource confusion: Sonic is explicitly using the dark decoded resource in the captured scene phase.
- Added `scripts/build-sonic-bridge-object-material-identity.ps1` to join the packet timeline, material summary, GVRT contact sheet, and texture-bind trace into one focused row. The identity artifact in `artifacts/compat-runs/20260601-sonic-bridge-texture-decoded-resource/bridge-object-material-identity` ties packet `0x813184D0` / object `0x813184E8` to draw `12188`, runtime texture `0x0072C600`, GVRT payload `0x8137DFA0`, and nearest texture bind FIFO `0x2D1D88` (`PC=0x801037A4`, `LR=0x80103988`). This gives the bridge investigation a stable checkpoint: the renderer is seeing the intended dark decoded material for that object in this scene phase, so the next useful work is scene/camera phase or Dolphin-side material expectation rather than another texture copy/bind trace.
- Added `scripts/find-sonic-gvrt-runtime-matches.ps1` to sweep all existing runtime texture-dump indices for the decoded `GVRT` mip0 hashes. The sweep in `artifacts/compat-runs/20260601-sonic-bridge-texture-decoded-resource/gvrt-runtime-sweep` scanned three `textures/index.csv` artifacts and found eight matches, all for the dark `0x8137DFA0` resource at runtime address `0x0072C600` across draws `12181..12188`; the five brighter neighboring resources still have zero runtime matches. This further points the next compatibility pass toward scene phase/material expectation rather than same-window texture confusion.
- Added `sonic-gx-window-12140-materials` to the retail benchmark harness and `scripts/build-sonic-bridge-phase-report.ps1` for cross-run packet/material/texture phase summaries. The material run in `artifacts/compat-runs/20260602-002916/sonic-gx-window-12140-materials` reproduces the same bridge identity in one target directory: packet `0x813184D0`, object `0x813184E8`, material draw `12188`, texture `0x0072C600`, and eight mip0 hash matches for decoded payload `0x8137DFA0` across draws `12181..12188`. Re-running the global GVRT sweep now scans four texture indices and finds 16 total matches, still all for `0x8137DFA0`; the five brighter neighboring GVRT resources remain unused in all sampled runtime texture dumps.
- Added `scripts/build-sonic-dolphin-phase-report.ps1` and refreshed the material visual report against `artifacts/sonic-visual-alignment/20260531-164542`. The Dolphin phase report in `artifacts/sonic-visual-alignment/20260531-164542/dolphin-phase-report` classifies the current comparison as `same-frame-small-shift`: bridge, skyline, and lower-track all choose Dolphin `frame-00600-requested-00600`, with `dx` spread `0` and `dy` spread `16`. The focused bridge material still has black-write ratio `0.596157`, while the bridge crop luma is close to Dolphin (`47.23` candidate vs `42.53` sample, average delta `13.00`). This moves the next boundary away from broad attract-mode scene timing and toward local material/rendering expectations for the bridge object.
- Added `scripts/build-sonic-material-mask-report.ps1` and `sonic-gx-window-12180-efb-material` for same-coordinate material overlays. The mask report for `artifacts/compat-runs/20260602-002916/sonic-gx-window-12140-materials` shows draw `12188` occurs after the nearest display copy (`draws_seen=12180`, FIFO `+0x2D18B1`) and has zero projected overlap with the frame-600 bridge crop (`projected_material_bounds=-95.237/-220.490-684.254/128.423`, crop `116/156-536/366`). The EFB capture in `artifacts/compat-runs/20260602-014149/sonic-gx-window-12180-efb-material` confirms the material belongs to the post-copy EFB/next-frame skyline phase; its alignment run `artifacts/sonic-visual-alignment/20260602-015554` selects Dolphin frame 700 for bridge/skyline regions. The immediate next question is therefore the post-display EFB/next-frame renderer state, not the already-copied frame-600 bridge crop.
- Re-ran `sonic-gx-window-12180-efb-material` after enabling `gx-vertices.csv` for exact material masks. The exact-vertex run in `artifacts/compat-runs/20260602-021345/sonic-gx-window-12180-efb-material` reproduces the same material counters (`0x0072C600`, draw `12188`, 76,183 color writes, 45,417 black writes, black ratio `0.596157`) and the mask report confirms the material spans the top EFB band (`-95.237/-220.490-684.254/128.423`). The refreshed alignment in `artifacts/sonic-visual-alignment/20260602-022902` again picks Dolphin frame 700 for bridge and skyline, while lower-track still picks frame 600, so the post-copy EFB image is a mixed lifecycle point. The next useful implementation target is improving/validating EFB lifecycle and copy-clear/frame-source selection around the draw-12180 display copy, not another texture-resource trace.
- Added GX frame lifecycle metadata to `emulator-summary.json` plus `scripts/build-gx-frame-lifecycle-report.ps1` for offline classification from existing `gx-copies.csv`/`gx-coverage.csv` artifacts. Running it on `artifacts/compat-runs/20260602-021345/sonic-gx-window-12180-efb-material` writes `gx-frame-lifecycle-report/gx-frame-lifecycle-summary.json` and classifies the window as `post-display-copy-efb`: display copy `1` at draw `12180`/FIFO `+0x2D18B1`, first rendered draw `12181`, last draw `12204`, two texture copies after the display copy, and three clear events including the display-copy clear. This confirms draw `12188` is not part of the already-presented frame-600 XFB.
- Added diagnostic `--gx-frame-skip-copy-memory-writes` and target `sonic-gx-window-12180-efb-lifecycle` for current-EFB lifecycle probes that preserve copy-clear state while skipping expensive EFB copy writes to emulated RAM. Selected display-copy probes can now capture the XFB directly from the EFB copy parameters, so exact copy-index diagnostics do not need selected-display RAM readback.
- Added `-Configuration Release` to `scripts/run-retail-benchmarks.ps1` and promoted GX lifecycle fields into suite `summary.csv`/`summary.json`. The Release lifecycle probe in `artifacts/compat-runs/20260602-042508/sonic-gx-window-12180-efb-lifecycle` completed in `199.968s` and reported `post-display-copy-efb`, last display copy `1657` at draw `12180`/FIFO `0x2D18B1`, destination `0x004A2460`, two copy events afterward, three clears afterward, and `gxFrameRasterTevTextureMs=8933.185` out of `gxFrameReplayMs=10066.174`. After decoded texture-mip caching, the same Release lifecycle probe in `artifacts/compat-runs/20260602-101558/sonic-gx-window-12180-efb-lifecycle` completed in `183.275s`, with `gxFrameRasterTevTextureMs=3404.537` and `gxFrameReplayMs=3899.436`. After adding mipmapped direct fast sampling, `artifacts/compat-runs/20260602-105906/sonic-gx-window-12180-efb-lifecycle` completed in `114.329s`; the GX replay is now `784.357ms`, `gxFrameRasterTevTextureMs=375.404`, and all 24 CMPR triangles use the direct texture sampler.
- Added `sonic-gx-window-12180-display-copy` as a named presented-frame target for the draw-12,180 XFB. The target now uses exact copy `1657`, direct selected-display capture, skipped GX copy memory writes, and the mipmapped direct sampler, so the previously blocked warmed interval completes in Release: `artifacts/compat-runs/20260602-110123/sonic-gx-window-12180-display-copy` finishes in `105.778s`, captures source copy `1657` at XFB `0x004A2460`, and reports `display-copy` phase. The wide replay still spends `4361.692ms` in EFB copy/clear work, while raster TEV texture work is down to `379.952ms` with 1,287 direct sampler triangles (`CMPR`: 1,251, `RGB5A3`: 36). Comparing the fresh frame against `artifacts/dolphin-reference/20260531-122306/sonic-adventure-2-battle/samples` in `artifacts/sonic-presented-frame-compare/20260602-110123/20260602-110344` still shows a large whole-frame mismatch: best whole-frame sample is Dolphin frame 550 at `85.046%` changed, while frame 600 is `90.270%` changed. Shifted crop alignment in `artifacts/sonic-presented-frame-alignment/20260602-110123/20260602-110336` still selects Dolphin frame 600 for bridge/skyline/lower-track, with bridge `avgDelta=12.828`, lower-track `20.431`, and skyline `6.965`. The presented frame itself is therefore still a rendering/camera/projection mismatch, not only a post-copy EFB comparison artifact.

Workflow update:

- Added `scripts/run-retail-benchmarks.ps1` and `scripts/run-sonic-check.ps1` for repeatable Sonic/Pikmin compatibility probes. The harness applies a wall-clock watchdog, emits progress updates, captures auto-selected GX frames, EXI traces, GX copy CSVs, per-target `run.json` ledgers, and suite-level `summary.csv` / `summary.json`.
- Added `scripts/summarize-exi-trace.ps1` and `scripts/summarize-gx-copies.ps1` so old artifacts can be converted into compact milestone JSON without rerunning retail images. The EXI summary tracks memory-card command milestones such as `0x52` read-array sector reads; the GX summary tracks display/texture copy counts, nonblack display-copy milestones, and per-XFB destination lifecycles so black overwrites after useful frames are visible at a glance.
- Tuned the default 20M retail benchmarks to use lighter GX settings and skip copy CSV generation. The full copy-heavy path remains available through `-DeepGx`, but the routine four-target suite now stays useful as a bounded regression loop.
- Added `--run-summary <json-path>` and wired it into the retail benchmark harness. Each target now writes `emulator-summary.json`, and `summary.csv` includes executed instructions, stop reason, and final PC without requiring console-log parsing.
- Added selected GX frame source metadata to `emulator-summary.json` and made `scripts/run-retail-benchmarks.ps1 -Targets sonic-20m,pikmin-20m` split comma-separated target lists before validation.
- Added phase timings to `emulator-summary.json` and surfaced the main columns in the suite `summary.csv` / `summary.json` so each run reports total time, emulation time, post-emulation time, and per-diagnostic buckets such as GX frame rendering, copy/coverage/TEV/texture dumps, memory dumps, and profile output.
- Added `scripts/run-retail-benchmarks.ps1 -PerfOnly` for quick speed probes. Perf-only runs keep `--quiet`, `--no-registers`, and `emulator-summary.json`, disable routine GX/EXI diagnostics, and write a compact `performance.csv` with process MIPS, emulation MIPS, diagnostic share, stdout/stderr byte counts, and filtered `pcProfileHead` values for the `sonic-profile-5m`, `pikmin-profile-5m`, and `mariokart-debug-profile-5m` targets. Profile targets now also auto-generate `profile-clusters/profile-clusters.csv` unless `-SkipProfileClusterReport` is supplied.
- Added nested GX frame renderer timings for FIFO expansion, replay, vertex decode, rasterization, EFB copies, display-copy capture, and PNG writing. The suite summary includes the headline renderer columns so optimization runs can identify whether `gxFrameDumpMs` is CPU replay, raster work, copy handling, or output I/O.
- Optimized the software rasterizer's triangle inner loop to step edge functions incrementally and reuse a per-triangle reciprocal instead of recalculating all edge weights and divisions for every candidate pixel. The `pikmin-5m` frame hash stayed identical, while raster time dropped from roughly 8.47s to 6.50s on the measured run.
- Added a direct 1:1 EFB display-copy path for unscaled, unfiltered, in-bounds display copies. The `pikmin-5m` frame hash stayed identical, but timing evidence was noisy while other emulator workloads were active.
- Split GX frame raster timings into setup, coverage, depth test, TEV/texture, alpha test, and blend/writeback buckets. A `pikmin-5m` probe showed TEV/texture work dominating the raster bucket (`rasterTevTextureMs` roughly 6.18s of 8.20s) while preserving the prior frame hash.
- Added GX frame fast-path counters plus cached single-stage TEV/direct texture sampling paths, including snapshot-backed IA/CI/TLUT texture ranges. A `pikmin-5m` probe preserved the frame hash and reported 40 single-stage TEV triangles, 4.43M fast TEV pixels, 22 direct IA4 texture triangles, and `rasterTevTextureMs` around 4.92s on the measured run.
- Added EFB copy sub-counters, mode signatures, no-scale filtered display-copy fast paths for Sonic and Pikmin, an exact Sonic display-filter path, an exact Pikmin display-filter path, and a bulk I4 texture-copy path. A `sonic-20m` probe stayed frame-hash identical while dropping `gxFrameEfbCopyMs` from roughly 15.9s to 3.15s; a `pikmin-5m` probe stayed frame-hash identical while dropping `gxFrameEfbCopyMs` from roughly 452ms to 259ms. The summaries show Sonic's copy load is 162 filtered display copies using filter `8/8/10/12/10/8/8`, 322 I4 texture copies, and 484 clears, while Pikmin's early copy load is 14 filtered display copies using filter `5/6/14/14/14/6/5`.
- Cached constant-white texture fallback inside the single-stage TEV fast evaluator for disabled, invalid, or incomplete texture inputs. The `pikmin-5m` probe stayed frame-hash identical while dropping `gxFrameRasterTevTextureMs` from roughly 5.10s to 4.07s on the measured run; the stats still report the same 40 single-stage TEV triangles, 22 direct IA4 texture triangles, and 18 incomplete-texture fallback triangles.
- Cached TEV swap-table decode inside the single-stage TEV fast evaluator, avoiding per-pixel BP register lookups for raster and texture channel swaps. The `pikmin-5m` probe stayed frame-hash identical again, with `gxFrameRasterTevTextureMs` measuring roughly 2.93s on the validation run.
- Collapsed fast TEV color evaluation from three per-channel input decodes to one shared RGB input decode per pixel. `pikmin-5m` stayed frame-hash identical with `gxFrameRasterTevTextureMs` measuring roughly 1.70s on the validation run; `sonic-20m` also stayed frame-hash identical against the prior Sonic baseline.
- Reused raster setup's cached stage-0 texture coordinates in the single-stage TEV fast evaluator, avoiding per-pixel vertex texcoord lookups for direct texture sampling. `pikmin-5m` and `sonic-20m` stayed frame-hash identical; the Sonic validation run measured `gxFrameRasterTevTextureMs` at roughly 1.21s versus roughly 2.14s before this pass.
- Added a narrow Pikmin heap-wait fast-forward for the `0x800466C0..0x800466C8` poll loop. It advances emulated hardware time to video interrupt opportunities instead of modifying the waited-on game flag directly. The 20M Pikmin benchmark now moves from the heap wait at `0x800466C8` to runtime code around `0x80045B90`, with GX FIFO capture increasing from roughly 230K to 549K bytes.
- Added optional `mariokart-debug-5m` and `mariokart-debug-20m` benchmark targets. The debug build first exposed missing CPU `mffs`; after implementing it, the next blocker was its JAS/DSP task-ready callback at `0x800A4880`, which now completes through a recognized byte-set callback shape instead of spinning at `0x800A49A0`/`0x800A5040`. The current wall is the debug image's MetroTRK/NUB path, so the Mario Kart targets skip GX frame/copy dumps until execution reaches game-rendering code.
- Mario Kart's debug image is useful as a CPU/DSP/debug-runtime probe, but not yet as a rendering benchmark: MetroTRK/NUB startup dominates before GX FIFO traffic appears. Added exact fast-forwards for the SDK `strlen` leaf, the varargs register-save stub, and trivial `return 0` leaves; a 20M probe now reports roughly 2.9M string-length and 5.8M small-leaf instructions skipped while still ending inside debug monitor startup with zero GX FIFO bytes.
- A deep Sonic 20M GX probe reached 296 display copies and 589 texture copies before the 360s watchdog. It found two alternating XFB destinations (`0x004A2460` and `0x00538460`) with 97 nonblack copies each and 49 later black overwrites each; the first auto frame preserved a partial SEGA logo from the last useful nonblack copy before later selection work promoted the fuller largest-display-copy path. A deep Pikmin 20M probe completed in 188.9s with 612 display copies at `0x003FE7A0`, 11 nonblack copies, and 600 later black overwrites; the auto frame preserves the Nintendo logo from the last useful nonblack copy.
- A CPU-only Pikmin 20M timing probe completed in 82.6s, while deep GX copy diagnostics add more than two minutes on the same path. Routine Pikmin targets now skip copy CSVs unless `-DeepGx` is requested; the default `pikmin-20m` harness path now completes in 126.2s while still capturing the auto frame and selected frame-source metadata.
- Added GX copy-summary integration to the compatibility matrix and promoted `sonic-20m-deep` to a repeatable slow target. The latest deep Sonic probe completed 20M instructions in 172.5s with 547 display copies, 1,092 texture copies, 194 nonblack display copies, and two alternating XFB destinations that are later overwritten by black frames; the largest display copy had 244,923 nonblack pixels. `pikmin-20m` is now baselined at final PC `0x801FBF18`, 549,231 FIFO bytes, and 29 rendered quads without locking a PNG hash because the diagnostic frame still exhausts the raster budget.
- Changed GX `auto` frame selection to prefer the largest nonblack display copy over the latest nonblack display copy when the final EFB is black. This makes Sonic's routine auto screenshot pick copy 602 at `0x00538460`, the fuller useful XFB, while copy diagnostics still preserve the later black-overwrite lifecycle.
- Added structured PC and indirect-call profile summaries to `emulator-summary.json` / `run-summary.json`, then promoted `sonic-resource-20m` as a CPU-only diagnostic matrix target. The first run completed in 148.4s at `0x81230FF8`, fast-forwarded 86,263,680 PRS decompression instructions and 622,809 resource-table lookup instructions, traced repeated `0x80116B9C` lookups for keys around `0x0089549A..0x008954A3`, and identified `0x800E78AC` / `0x800E78D4` as the hottest sampled PCs. Those PCs are external-interrupt MSR helper leaves, so their fast-forward counter is now tracked separately from generic small-leaf helpers and the matrix now also reports the top PC after filtering those leaves.

Next useful targets:

- For Sonic, inspect whether the dim/low-alpha UI is expected fade behavior or a TEV/blend accuracy issue. Good next probes are TEV sample diagnostics for representative early and late windows, display-copy YUYV range comparison, and windowed `last-display-copy` sweeps across draw ranges instead of one huge global raster budget.
- Focus the next Sonic resource investigation on the filtered top PCs, currently the `0x800F1438`/`0x800E2E34` task/resource staging path. The `sonic-resource-20m` target now branch-profiles the `0x800E3128`, `0x800E3130`, and `0x800E316C` sites so we can see the exact helper chain before considering any exact fast-forward. The latest 20M run showed deterministic targets: `0x800E3128 -> 0x800E2C0C` 50,699 times, `0x800E3130 -> 0x800F13A8` 50,696 times, and `0x800E316C -> 0x800E2E20` 50,698 times. The `0x800E2E20` helper uses a jump table into broader resource/task code, so it should not be skipped wholesale.
- Expanded the Sonic resource profile to include internal branch sites inside `0x800E2C0C` and `0x800F13A8`; the matrix `branchSiteTargets` column now preserves a compact site-to-top-target summary for all profiled branch sites in the target.
- Added an exact fast-forward for Sonic's `0x800F13A8` interrupt-guarded resource mode query. It preserves the caller-visible mode result and interrupt state, skips about 2.09M instructions in the 20M resource profile, and moves that target's endpoint forward to `0x8012347C`; the next hot sampled PCs are now around `0x800E3134` / `0x800E2E20`, so the remaining work is the state/resource helper chain rather than the mode query itself.
- Added structured Sonic resource-state diagnostics for the global block at `0x801CC168` and expanded branch profiling around the `0x800E3134` decision block plus the `0x800E2E20` state/jump-table helper. Use the resulting `sonicState*` columns and `branchSiteTargets` rollup to decide whether the remaining hot path is a safe polling helper or a state transition that depends on a missing subsystem callback.
- Added an exact fast-forward for the observed `0x800E2E20` state-poll arm. In the 20M resource profile it now skips about 1.27M state-poll instructions, keeps `sonicStateByte47=0`, and moves the endpoint to `0x800E78AC`; the remaining dominant sampled PCs sit in the `0x800E30CC` caller path and external-interrupt helper traffic.
- Added `--profile-pc-lr` to profile LR values observed at hot helper PCs. In the current Sonic resource profile, `0x800E78AC` is most often reached with `LR=0x800F1374` and `0x800E78D4` with `LR=0x800F1390`, both inside the compact `0x800F135C` mode wrapper; the next largest LR sources are `0x800E6760` and `0x80170440` subsystem paths.
- Added an exact fast-forward for Sonic's compact `0x800F135C` mode wrapper. The 20M resource profile now skips about 258K wrapper instructions, keeps the prior PRS/resource/state-poll counters stable, and advances the endpoint to `0x80BC8118`. The hot external-interrupt LR source shifts to `0x800E679C` / `0x800E68BC`, so the old `0x800F1374` caller expectation was retired and replaced with the next visible caller profile.
- Added a general SDK timebase-read leaf fast-forward for the `mftbu; mftb; mftbu; cmp; bne` stable-read helper and the short `mftb; blr` leaf. The Sonic 20M resource profile keeps the same resource counters while advancing the endpoint to `0x80BCC050`; the matrix now tracks those skips separately as `timeBaseReadInstructions`.
- Added `scripts/compare-run-summaries.ps1` and the focused `sonic-resource-hot-3134` matrix target so resource-path experiments can stop on the current hot phase and compare changed counters/hotspots without manually diffing console logs.
- Added DI and EXI debug snapshots to run summaries and surfaced the key interrupt, disc-command, and channel-0 state in the compatibility matrix. The current Sonic hot probe now shows PI cause `0x00000004`, DI status `0x0000003A`, command `0xA8000000`, and no pending DI command at the stop point, while EXI channel 0 is idle with no selected device.
- Added a recent PI/DI MMIO access tail to run summaries and matrix CSVs. Sonic's hot probe reads `DI_STATUS=0x3A`, writes `0x2A` to preserve DI masks without clearing TCINT, and starts the next `0xA8` read, which matches Dolphin's W1C DI status handling and shifts the next investigation toward whether our read timing/completion ordering is too eager.
- Added DI command-history snapshots, the `--di-command-latency-cycles` diagnostic override, and Sonic 4K/32K latency sweep targets. The default and 4K probes both stop on the same DI-complete phase, but the 32K probe stops with a pending `0x80000`-byte read and PI cause shifted to video, so the next DI work should replace the fixed command latency with transfer-size-aware timing.
- Replaced the fixed default DI command latency with a transfer-size-aware model while keeping `--di-command-latency-cycles` as an override. Sonic's default hot probe now stops with a pending `0x80000`-byte read, matching the useful 32K timing bracket instead of the old eager fixed-10K completion path.
- Added pending DI command summaries to run summaries and matrix CSVs, then refreshed the retail baseline PCs for the transfer-size-aware DI timing model. Pikmin 5M keeps the prior frame hash, Pikmin 20M still exceeds the GX/quads thresholds, and Mario Kart Debug remains in debug-runtime startup with only an Inquiry command completed.
- Added watch hit counters to run summaries/matrix CSVs plus the focused `sonic-resource-final-read-watch` target. The exact final pending DMA range `0x812FFE60..0x8137FE5F` reports zero load hits before the hot-stop fires, so Sonic is not polling the partially filled DVD buffer directly in that phase. A matching no-hot-stop 17M probe lets the command finish and shows reads #29..#36 completing, ending with no pending DI command at `0x800EEF6C`; the target is now captured as `sonic-resource-di-completion-17m` for future reruns.
- Added `--profile-after <n>` for late-window PC/branch/LR/call profiles, then scoped `sonic-resource-di-completion-17m` to start profiling after `0x00F00000` instructions. The post-DI window now points at `0x800E81D8` and the surrounding `0x800E81A8..0x800E8358` cluster, while the old `0x800E3128` / `0x800E3130` / `0x800E316C` branch sites only fire once in that late slice.
- Added expanded PC trace registers plus the focused `sonic-resource-fixup-window-17m` target for the late `0x800E81A8..0x800E8358` resource relocation walker. A batched exact fast-forward now collapses that loop, reports about 917K `sonicResourceFixupInstructions` in the 17M probe, and moves the next sampled hotspot to the `0x800EEEFC..0x800EEF40` cluster with the endpoint at `0x80123410`.
- Refreshed `sonic-resource-20m` after the fixup fast-forward. The 20M endpoint now reaches `0x80169964`, PRS skips climb to about 157.7M instructions, resource lookup skips to about 2.5M, normalized string scans to about 1.6M, and the dominant remaining profile returns to the `0x800E3134` resource-task loop.
- Added an exact reverse word-fill fast-forward for Sonic's unrolled `0x80169960..0x801699A4` memory initialization loop. The late 20M resource probe reports 389,592 `reverseWordFillInstructions`, gets past the old `0x80169964` endpoint, reaches `0x8011DADC`, and profiles real overlay code loaded by DI at `0x80BC82B0`; a stop probe at `0x80BC82B0` confirms nonzero PPC instructions in RAM around 19.07M executed instructions.
- Implemented GQR-backed quantized paired-single load/store conversion for the immediate `psq_l/psq_lu/psq_st/psq_stu` forms, including u8/u16/s8/s16 scale, stride, truncate, and clamp behavior. Run summaries and register dumps now include `GQR0..GQR7`, and `--dump-disasm <addr> <count>` can disassemble live RAM overlays after a stop; the `0x80BC82B0` Sonic probe shows all GQRs zero and the overlay tail looping over a 64-entry store/compare path before returning through `0x80BC82F4`.
- The widened live overlay dump shows the `0x80BC8110..0x80BC82B4` inner loop is mostly an inactive-slot scan: the branch at `0x80BC8128` skips the helper-call body for state values other than `1`. Added a guarded fast-forward for only that inactive path, leaving active slots and their helper calls emulated normally. The late Sonic probe now reports about 91K `sonicOverlayInactiveSlotScanInstructions`, moves the 20M endpoint to `0x8011DAA8`, and shifts the top sampled PC to the transform/decode cluster at `0x800EEF40`.
- A live dump of `0x800EED20..0x800EEFEC` shows the new `0x800EEF40` hotspot is a pathname/resource-table lookup over 12-byte records. The existing normalized string scan helper already collapses the inner character compare, but a first attempt at skipping the whole routine was unsafe and produced an early address-translation fault, so this path now needs differential validation before any routine-level fast-forward. The matrix target now keeps trace/disassembly coverage for the lookup body.
- Added `--trace-sonic-path-lookup <csv>` as an observational differential trace for that `0x800EECFC` lookup. On the 20M Sonic probe it recorded 56 calls, 56 model/result matches, zero mismatches, and zero model failures while preserving the known `0x8011DAA8` endpoint. A result-only fast-forward attempt still diverged, so the trace now also logs volatile register and CR side effects needed before enabling a routine-level skip.
- Expanded the path-lookup trace with actual instruction counts plus full GPR and caller-stack snapshots. The first 2M Sonic probe shows calls consuming roughly 7K to 77K PPC instructions each, so the failed routine-level skip was not just missing return registers; it also under-advanced emulated time by orders of magnitude. The next safe skip needs a model-derived instruction count or an audit mode proving the timing estimate before it can replace the real routine.
- Added elapsed timebase/decrementer deltas, interrupt-entry counts, and a first table-walk cycle estimator to the path-lookup trace. The 2M calibration probe still has per-call cycle error, especially on interrupt-preempted calls, so routine-level skipping should stay disabled until the estimator is tighter or the skip is guarded to calls that cannot cross an interrupt boundary.
- Tried a conservative active path-lookup skip guarded by pending interrupt state, DI completion distance, VI interrupt distance, decrementer margin, and a 32K-cycle estimate cap. It fired on two early 2M calls and kept the run alive, but the 20M endpoint shifted to `0x80103E68` instead of the known `0x8011DAA8`, so the active skip remains disabled. The trace now records `fast_forward_eligible` and summary `eligibleFastForwards` so future runs can audit candidate calls without changing execution.
- Re-enabled the Sonic path lookup fast-forward with a tighter cycle estimator. It now only skips calls whose modeled duration stays below the DI/VI interrupt safety window, preserves the observed return-value side effects (`r0`, `r3`, `r4`, and `PC/LR`), and leaves borderline lookups such as `dummy.bin` emulated. A short 1.2M trace recorded 16/16 matching path lookups with one eligible skip and no mismatches.
- Added `sonic-title-probe` to the retail benchmark harness as the longer 50M Sonic visual target using `largest-display-copy`, plus summary columns for the path lookup, path record scan, resource lookup, and PRS fast-forward counters.
- The first `sonic-title-probe` run completed 50M instructions in 568s and selected copy 602, a clean SEGA splash with 148,025 nonblack pixels. The new display-activity summary shows two visible bursts followed by 382 black display copies while draw count rises from 202 to 120,791, so the next target is late-scene/presentation state rather than copy selection.
- `scripts/summarize-gx-copies.ps1` can now emit a compact `gx-display-activity.csv`, and the retail harness writes it next to `gx-copies.summary.json` for deep GX targets.
- Added a narrower Sonic path record-scan fast-forward at `0x800EEEC4`. It only skips proven nonmatching 12-byte records, stops before matching or unknown candidates, and leaves the whole-routine path lookup skip disabled. In the 20M Sonic probe it reports 4,266,840 `sonicPathRecordScanInstructions`, drops normalized string-scan skips to 19,872, and moves the sampled hotspot to the paired-single transform cluster around `0x8011DAD8..0x8011DADC` with endpoint `0x80120EB0`. A follow-up path trace still records 56 modeled lookups, 56 matches, zero mismatches, and zero model failures.
- Added full GPR and paired-single FPR lane snapshots to register dumps/run summaries, plus the focused `sonic-transform-8011daa8` matrix target. The first stop probe hits the transform-entry loop at `0x8011DAA8`; an earlier pre-fast-forward probe at `0x8011DAD8` showed `CTR=0x16`, `r4=0xE0000090`, input stream `r6=0x81337340`, output stream `r5=0x80B28518`, and `GQR1=0x08040804`, confirming this wall is a tight paired-single vertex transform/decode loop rather than the old pathname lookup.
- Added exact fast-forwards for the Sonic paired-single 2D transform loop at `0x8011DAA8` and the follow-on GX float strip emit loop at `0x8011D610`. A clean-path GX draw-begin helper now skips the zero-dirty-flag case at `0x80101948`, and a second GX emitter fast-forwards the float plus halfword-attribute strip loop at `0x8011D860`.
- Added an exact fast-forward for the warmed paired-single 4D transform loop at `0x8011DB94`; it models the rolling f15..f18 output stores, advances the input/output cursors, and restores f14..f18 from the caller stack before returning.
- The current 20M Sonic probe reports 146,506 `sonicPairedTransform2dInstructions`, 44,296 `sonicPairedTransform4dInstructions`, 51,436 `sonicGxFloatStripEmitInstructions`, 87,332 `sonicGxDrawBeginInstructions`, and 157,698 `sonicGxFloatTexcoordStripEmitInstructions`, reaching overlay code at `0x80BCC038`. Remaining sampled hotspots are dirty-state draw-begin calls at `0x80101948` and the `0x80120D98..0x80120EA4` loop cluster.
- Added `--trace-sonic-vertex-provenance` for targeted FIFO-to-source attribution and captured `artifacts/compat-runs/20260530-sonic-vertex-provenance-bridge`. The first 93 bridge vertices at FIFO `0x2D1E42..0x2D25A5` all infer packet `0x813184D0`; every decoded stream index matches the transformed source record index, and every decoded halfword attribute matches the live emitter GPRs. The first vertex, for example, maps stream record `0x8131768C` (`000F0608FDD8`) to source record `0x80B286E0` (`x/y/z = 0x4420EB31/0x45208C27/0xC2A2CEA0`) and then to identical FIFO payload bytes. This clears the GX emitter and stream-index decode for the bridge span; the remaining mismatch is upstream in the transformed vertex table producer, source input data, or scene/camera timing.
- Added `scripts/build-sonic-vertex-lineage.ps1` and captured a same-run bridge lineage in `artifacts/compat-runs/20260530-sonic-bridge-lineage-focused`. All 93 bridge provenance rows now map to transformed-table writes at `PC=0x8011DAA8`, with source words matching the post-write table bytes, and all 93 overlap the same transform-input row (`output_cursor=0x80B284F8`, `input_cursor=0x81317DC8`, `iterations=0x71`, `GQR1=0x08040804`). The immediate next target is therefore the packed input stream/matrix feeding that `0x8011DAA8` transform, not GX FIFO emission.
- Widened Sonic transform-input captures to include the full focused transform batch instead of a fixed `0x100`-byte slice, then captured `artifacts/compat-runs/20260530-sonic-bridge-lineage-wide-input`. `scripts/summarize-sonic-transform-source-map.ps1` maps the bridge output records back to packed input records; for example FIFO `0x2D1E42` / source record `0x80B286E0` is output index 15 from packed input `0x81317E98` with source coordinates roughly `2114.69, 2968.54, -875.93`, transformed to `643.67, 2568.76, -81.40`. This confirms the packed input stream contains plausible bridge/object coordinates and the skew is introduced by the transform matrix/state used at `0x8011DAA8`.
- Added `scripts/fit-sonic-transform-affine.ps1` to turn a Sonic transform source map into a fitted 3x4 affine matrix and residual report. Fitting `artifacts/compat-runs/20260530-sonic-bridge-lineage-wide-input/sonic-transform-source-map.csv` reproduces 86 mapped bridge vertices with `0.000182` average residual and `0.000416` max residual. The fitted rows are `x=(-0.930793,0.977714,-0.421544,-659.618)`, `y=(0.04547,0.977714,-0.204956,-609.311817)`, and `z=(0.211761,-0.20994,-0.954507,-742.081769)`, matching the live f0..f7 transform lanes. Row lengths/dots are `1.414214/1/1` and `1/0/0`, so the emitted bridge vertices are explained by one stable but sheared CPU-side matrix rather than random vertex decode drift.
- Captured the provenance-path locked-cache matrix writes in `artifacts/compat-runs/20260530-sonic-bridge-matrix-provenance-path`. The matrix feeding the bridge transform is written immediately before the transform by the `0x8011C164..0x8011C184` `psq_st` sequence into `0xE0000090..0xE00000BF`; the final written bytes decode to the same `f0..f7` pairs observed at transform entry (`f0=-0.93079/0.04547`, `f1=0.21176/1`, `f2=0.97771/0.97771`, `f3=-0.20994/1`, `f4=-0.42154/-0.20496`, `f5=-0.95451/1`, `f6=-659.62/-609.31`, `f7=-742.08/1`). The next debugging boundary is the producer of that locked-cache matrix, not the paired-transform execution or the packed vertex stream.
- Added `scripts/build-sonic-matrix-producer-timeline.ps1` and fixed the locked-cache summary's float decoding. The focused slot-range run in `artifacts/compat-runs/20260531-sonic-bridge-matrix-slot-producer-v2` captures parent/current/transform slots together (`0xE0000030..0xE00000BF`) through bridge packet `0x813184D0` and neighbor `0x81318FC8`. Both packets inherit the same parent slot translation `-228.988/-130.833/-837.968` and same rotation row shape; the bridge-specific step at `0x801161C4` applies object pointer `0x813184F0` to produce current-slot translation `-659.044/-609.046/-744.443`, then `0x8011C184` transposes it for the transform. The neighbor follows the identical path from the same parent, but object pointer `0x81318FE8` produces `108.343/-536.090/2801.898`. This clears the local matrix producer chain as internally coherent; remaining bridge mismatch work needs a Dolphin-aligned camera/object reference or a same-scene visual alignment check, not another same-run matrix trace.
- Added `scripts/summarize-sonic-transform-lanes.ps1` to decode the matrix-writer FPR pairs in the lane order consumed by Sonic's vertex transform path. Running it on `artifacts/compat-runs/20260530-sonic-bridge-matrix-writer/sonic-matrix-writer.csv` writes `sonic-transform-lanes.csv`: packet `0x813184D0` reaches the terminal transform handoff with source and packed rows `x=(-0.930793,0.977714,-0.421544,-659.617981)`, `y=(0.04547,0.977714,-0.204956,-609.311829)`, and `z=(0.211761,-0.20994,-0.954507,-742.081726)`. The same basis is already present at `0x801160DC` when the parent matrix is copied into the current slot, so the bridge skew is not introduced by the final transpose/packing step.
- Added `scripts/run-sonic-matrix-producer-probe.ps1` and widened `scripts/build-sonic-matrix-producer-timeline.ps1` to decode a fourth 0x30-byte slot when tracing from `0xE0000000`. The bridge-root run in `artifacts/compat-runs/20260531-sonic-matrix-producer-bridge-root` shows the bridge parent slot is first copied by the generic reverse-copy helper at `0x8010C07C..0x8010C0C4` from the lower/root slot, then copied into the object current slot by `0x801160B8..0x801160DC`. A root-only run in `artifacts/compat-runs/20260531-sonic-root-slot-producer` traces the root slot itself back to Sonic's paired-single affine multiply routine at `0x800ED368..0x800ED430`; added a unit test proving that routine matches a plain 3x4 affine-multiply reference under our interpreter. The current boundary is now the camera/root matrices fed into `0x800ED368`, not paired-single opcode semantics, final transpose, object translation, stream decode, or GX emission.
- Added `--trace-sonic-root-matrix <csv>` plus `scripts/summarize-sonic-root-matrix.ps1` to capture Sonic's root/camera matrix builder inputs. The first root run in `artifacts/compat-runs/20260531-sonic-root-matrix-inputs` showed the bad bridge basis came from the rotation-builder left matrix `x=(1,1,0,0)`, not from the affine multiply. A tighter builder run then isolated the corruption to `ps_merge00 f4, f2, f4`: the destination aliased source B, and the interpreter overwrote lane 0 before reading source B's lane 0. `PairedSingleMerge` now snapshots both source lanes before writing, with a regression test for the alias case. The confirmation runs in `artifacts/compat-runs/20260531-sonic-root-rotation-merge-fix` and `artifacts/compat-runs/20260531-sonic-bridge-after-psmerge-fix` show the root rotation terminal is identity and the bridge packet `0x813184D0` now reaches the transform handoff with orthonormal rows `len=1/1/1`, `dot=0/0/0` and translation `-49.998/-609.046/-744.443`.
- Added `scripts/search-sonic-visual-alignment.ps1` for shifted same-scene crop searches against Dolphin samples, with nonblack/luma coverage columns so black fade frames do not rank as false positives. The filtered run in `artifacts/sonic-visual-alignment/20260531-122030` picks Dolphin frame 600 as the closest real sample: bridge best is `dx=8, dy=0` at `18.184` average delta but `99.805%` changed, skyline best is `dx=32, dy=-8` at `8.620` average delta and `86.371%` changed, and lower-track best is `dx=-88, dy=-24` at `25.992` average delta and `99.828%` changed. A longer Dolphin reference pass, `artifacts/dolphin-reference/20260531-122306`, produces the same nearest frame-600/frame-650 result in `artifacts/sonic-visual-alignment/20260531-122440`. This rules out a simple global screenshot shift or sparse reference-sample timing issue; the remaining Sonic bridge mismatch is in camera/projection/object transform state.
- Added `-ExtraRunArgs` to `scripts/run-sonic-visual-anchor.ps1` and ran two bridge A/B captures against `artifacts/dolphin-reference/20260531-122306`. `artifacts/sonic-visual-anchor/20260531-130031` disables only `--disable-sonic-paired-transform-fast-forward`; `artifacts/sonic-visual-anchor/20260531-131930` disables the full `--disable-sonic-geometry-fast-forward` family. Both candidate PNGs hash exactly the same as `artifacts/sonic-visual-anchor/20260529-020136/candidate.png`, and `compare-images` reports `0` changed pixels. The second run also reports zero paired/vector/generated Sonic geometry fast-forward counters, clearing the geometry fast-forward helpers as the cause of the bridge skew.
- Expanded GX coverage rows with projected/clipped vertex counts and near-clip triangle counters. This turns the Sonic bridge window into a renderer-side triage artifact: rows with high `clipped_vertices` and high `near_clip_culled_triangles` can now be separated from rows that rasterize and then lose pixels to depth, alpha, color-update state, or black TEV output.
- Re-ran `sonic-gx-window-12140-provenance` in `artifacts/compat-runs/20260531-231255` after adding the counters. The run completes 50M instructions in 645s and keeps the grouped transform fits clean: packet `0x813184D0` has 273 points, `0.000169` average residual, row lengths `1.000000086/1.000000096/1.00000002`, and near-zero row dots. The new coverage summary reports 446 clipped vertices, 343 clip-input triangles, 60 generated near-clip output triangles, and 301 near-clip culled triangles. In the bridge span, draw `12188` is the first large rasterizing bridge write (`76,183` covered pixels, `45,417` black writes, `30,766` nonblack delta), while many adjacent strip draws generate near-clip output but still rasterize as degenerate/offscreen. This keeps the current boundary in renderer clipping/raster diagnostics rather than CPU transform or FIFO provenance.
- Added `--dump-gx-triangle-coverage <csv-path>` and enabled it for the `sonic-gx-window-12140-provenance` target. The new CSV replays the same bounded GX window while preserving EFB/depth state and emits one row per primitive triangle with source vertex indices, near-clip counters, raster/color/depth/alpha counters, and a coarse render/culled reason. This should make the next bridge run precise enough to distinguish bad strip winding or clipping output from valid offscreen geometry.
- Re-ran `sonic-gx-window-12140-provenance` in `artifacts/compat-runs/20260531-235343` with triangle coverage enabled. `gx-triangle-coverage.csv` contains 727 primitive-triangle rows: 48 `rendered`, 301 `near-clip-culled`, 42 `near-clip-degenerate-or-offscreen`, and 336 `degenerate-or-offscreen`. The rendered rows account for essentially all draw coverage (`307,234` covered pixels, `307,230` color writes, `58,313` black writes). In the first bridge draw, `12188`, only triangles 5-10 render; they cover the visible central band while triangles 20-26 are fully near-clip culled and triangles 18-19/27-30 produce near-clip output entirely offscreen. This points the next renderer pass at projected bounds/frustum behavior and TEV output color for the rendered band, rather than triangle-strip winding.
- Triangle coverage rows now include centroid TEV/color samples, raw Z/blend/alpha/gen-mode state, and the detailed stage summary. The refreshed run in `artifacts/compat-runs/20260601-030731` shows bridge draw `12188` uses stage0 `Modulate` with bright raster colors (`172/170/167` or `213/213/211`), then samples mostly black/dark texels from CMPR texture `0x0072C600` (`64x64`, `linear:linear-mipmap-linear`, `lod=bias0:min0:max6`) and modulates down to `0/0/0`, `0/8/12`, `0/7/10`, or `6/12/12`. `scripts/summarize-gx-triangle-coverage.ps1` now writes `gx-triangle-coverage.summary.csv/json`; the summary reports 48 rendered triangle rows, 307,230 color writes, 58,313 black writes, and 6 dark rendered rows, with the `0x0072C600` bridge texture group responsible for 76,183 covered pixels and 45,417 black writes. The next target is therefore texture sampling fidelity for this CMPR/mipmapped material, not vertex color or TEV combine selection.
- Texture sampling diagnostics now walk the CMPR/RGB mip chain by level and record `texture_mip_samples` in triangle coverage summaries. The Sonic run in `artifacts/compat-runs/20260601-105619` shows the bridge texture `0x0072C600` remains dark across all sampled mips: for the heavy black-write triangles, mip0 is black or dark blue-green and lower mips stay around `0..9` red, `0..14` green, and `0..20` blue. This clears "base level only" as the main explanation for the bridge band.
- The software renderer now estimates per-triangle texture LOD for mipmapped textures, samples the selected mip level or mip blend in the generic TEV path, and keeps the direct single-stage fast texture sampler disabled for mipmapped textures until it has equivalent LOD support. A CMPR regression renders black base level versus red mip level correctly. Re-running `sonic-gx-window-12140-provenance` in `artifacts/compat-runs/20260601-111620` keeps the bridge counters unchanged (`307,230` color writes, `58,313` black writes; `0x0072C600` still contributes `76,183` covered pixels and `45,417` black writes), so remaining Sonic bridge work should move to projection/clipping/presentation state or Dolphin-aligned material expectations rather than mip addressing.
- Perspective near-plane enforcement now uses the projection matrix's near term during vertex transform, and triangle coverage rows include `view_w_min`, `view_w_max`, and `near_clip_w`. A first derivation attempt using `projection_23 / projection_22` was rejected by the Sonic bridge run because Sonic's active projection has `projection_22` near zero and `projection_23=-1`; the renderer keeps the established `abs(projection_23)` near distance instead. The corrected run in `artifacts/compat-runs/20260601-123917` keeps the Sonic bridge counters unchanged and shows draw `12188` visible triangles are safely in front of the near plane (`near_clip_w=1`, rendered-triangle `view_w_min` about `3198..3569`). The offscreen/cut part of the strip crosses negative `view_w`, but the visible dark band is not a near-plane clipping artifact.
- Triangle coverage rows and summaries now include stage0 texture-coordinate bounds. The refreshed Sonic run in `artifacts/compat-runs/20260601-125602` confirms the visible dark bridge triangles in draw `12188` sample coherent wrapped UV ranges rather than a single bad centroid: the six `0x0072C600` rendered triangles cover roughly `s=1.172..5.531` and `t=-7..-4.555`, with centroid samples still using mip level 0. That leaves material/texture content, UV source data, or scene timing versus Dolphin as the next useful boundary.
- Added `scripts/summarize-gx-materials.ps1` and wired it into the retail benchmark harness after triangle coverage summaries. On the latest Sonic bridge run it writes `gx-materials.summary.csv/json`; the top material is `0x0072C600` (`CMPR`, `64x64`, `linear:linear-mipmap-linear`, `Modulate`, `selected0`) with 6 triangles, 76,183 covered pixels, 45,417 black writes, `59.6%` black-write ratio, UV range `s=1.172..5.531`, `t=-7..-4.555`, raster colors split between `172/170/167` and `213/213/211`, and TEV outputs mostly `0/0/0`. This is now the canonical compact artifact for comparing the material against Dolphin.
- Added `scripts/build-sonic-material-visual-report.ps1` to join that material summary with the shifted Dolphin visual alignment artifact. For `artifacts/compat-runs/20260601-125602/sonic-gx-window-12140-provenance`, it selects texture `0x0072C600` and pairs it with the bridge crop from Dolphin `frame-00600-requested-00600` in `artifacts/sonic-visual-alignment/20260531-164542`: shifted-crop average delta is `13.004`, Dolphin crop average luma is `42.527`, and ngcSharp crop average luma is `47.231`. The bridge crop is close in broad brightness after the paired-single merge fix, while the material-level black writes remain concentrated in the `0x0072C600` band.
- `--dump-gx-textures` now emits mipfiltered texture bindings one image per mip level and tags `index.csv` with `mip_level`. That gives the next Sonic pass a direct PNG view of texture `0x0072C600` and its lower levels, rather than relying only on centroid mip samples embedded in triangle/material summaries.
- Added `scripts/build-sonic-bridge-material-timeline.ps1` to join `gx-materials.summary.csv`, texture-dump `index.csv`, and `sonic-packet-timeline.csv` into a bridge-focused CSV/JSON plus contact sheet. Running it against the latest artifacts confirms `0x0072C600` maps to packet `0x813184D0`, draw `12188`, has 7 decoded mip levels, and its base texture average luma is only `3.281`; the adjacent high-coverage city material `0x0071BC60` averages `46.529` and appears bright in the contact sheet. This strengthens the next hypothesis: the dark band is a real texture binding on the bridge packet, while the bright city texture is a separate nearby material, so the next trace should focus on why this object/material is selected at this scene phase.
- Fixed the GX projected-position path to use the console clip convention `-w <= z <= 0`, added clip-space columns to `gx-vertices.csv`, and quoted comma-bearing triangle-coverage fields so CSV import stays aligned. The narrow `sonic-gx-window-8302-materials` target crossed from effectively zero material coverage to 23 rendered triangle rows, 307,217 color writes, and 307,234 covered pixels in `artifacts/compat-runs/20260603-231051`; the suite `summary.csv` now mirrors those triangle-coverage totals even when draw-level coverage is disabled.
- Generalized clipped triangle replay from near-only clipping to the full diagnostic clip volume: near, far, and a small positive-W guard. A far-plane regression now covers the case where projection rejects one vertex but the remaining polygon should still render. The Sonic 8302 material counters stayed unchanged after this correctness pass, so the current 8302 blocker is not far/W clipping.
- `scripts/run-sonic-visual-anchor.ps1` now accepts `-Configuration Debug|Release`, matching the retail harness. The Release anchor in `artifacts/sonic-visual-anchor/20260603-230339` compares the current 8302/3900-draw candidate against Dolphin reference `20260531-122306`: full-frame best match is frame 550 at `85.046%` changed, skyline is `76.066%`, bridge is `99.415%`, and lower-track is `99.98%`. This is better than the older full-frame 88.211% baseline but still visually wrong in the important bridge/track crops.
- Added `scripts/build-sonic-visual-phase-report.ps1` to join visual-anchor crop scores, selected display-copy lifecycle, triangle coverage, and material summaries into one phase dashboard. Running it against the 8302 material probe correctly flags that the material window `8302..8332` ends `3848` draws before the selected display copy at draw `12180`.
- Re-ran the phase-overlapping `sonic-gx-window-12180-efb-material` target in `artifacts/compat-runs/20260603-234014` and generated `artifacts/sonic-visual-anchor/20260603-230339/visual-phase-report-12180`. This window overlaps the selected display-copy draw and still shows the same material split: `0x0072C600` on draw `12188` contributes 6 triangles, 76,183 covered pixels, 45,417 black writes, and a `0.596157` black ratio; `0x0071BC60` on draws `12199..12201` contributes 17 triangles, 231,024 covered pixels, and a `0.055822` black ratio. The material issue is therefore present in the displayed phase, not just in the earlier 8302 probe.
- Added `scripts/build-sonic-camera-material-report.ps1` for the displayed-phase `12180..12204` window. The report in `artifacts/compat-runs/20260603-234014/sonic-gx-window-12180-efb-material/sonic-camera-material-report` shows a single projection signature across all 24 draws (`perspective`, `projection_00=2.423`, `projection_11=3.231`, `projection_23=-1`, viewport scale/origin `320/-240/662/582`). The dark `0x0072C600` bridge material is draw `12188`, with 24 projected vertices, 10 clipped vertices, and very wide/offscreen screen bounds; the brighter `0x0071BC60` city material is draws `12199..12201`, with 10 projected vertices, zero clipped vertices, and mostly on-screen bounds. This narrows the mismatch toward object/material selection, geometry membership, or source scene state rather than per-draw projection state.
- Added `sonic-gx-window-12180-provenance-materials`, a focused displayed-phase provenance/material target. The run in `artifacts/compat-runs/20260603-235530/sonic-gx-window-12180-provenance-materials` emits packet timeline, scene state, matrix writer, transform source map, vertices, textures, and material summaries. Rebuilding the packet timeline with a 24-draw span maps the entire focused object range `12181..12204` to packet `0x813184D0`, object `0x813184E8`, object kind `0x16`, object position `-49.247711/-487.163788/0`, and matrix translation `-50.306/-609.312/-742.082`. Both the dark `0x0072C600` draw `12188` and brighter `0x0071BC60` draws `12199..12201` are therefore materials within the same object packet, not different scene objects.
- Added `scripts/build-sonic-material-source-report.ps1` and wired it into the retail harness whenever material, vertex, and transform-source CSVs are available. The report in `artifacts/compat-runs/20260603-235530/sonic-gx-window-12180-provenance-materials/sonic-material-source-report` shows the dark `0x0072C600` material and brighter `0x0071BC60` material overlap the same source/output range (`output_index` roughly `75..99`, input records around `0x81318258..0x813183D8`) and both have zero rejected rendered vertices. The exact overlap CSV shows four shared source records (`0x80B28EA0`, `0x80B28F80`, `0x80B29060`, `0x80B29140`), four dark-only records, and eleven bright-only records. The dark row is a real `CMPR 64x64` `repeat:repeat` sample with UV range `s=1.172..5.531`, `t=-7..-4.555`, while the bright city row is `CMPR 256x256` with `repeat:clamp` and `t=0..0.766`. This clears basic projection, clipping, and sampler wrap decoding as the cause of the dark band; the next boundary is Sonic's object material/partition selection or scene timing versus Dolphin, not the TEV sampler itself.
- Added `scripts/build-sonic-packet-material-partition-report.ps1` and wired it into the retail harness after the packet timeline/material-source summaries. The report in `artifacts/compat-runs/20260603-235530/sonic-gx-window-12180-provenance-materials/sonic-packet-material-partitions` reduces packet `0x813184D0` to five material sequences: draws `12181..12187` emit source records but render no material coverage, draw `12188` renders the dark `0x0072C600` partition, draws `12189..12198` emit a broader non-rendered source span, draws `12199..12201` render the lit `0x0071BC60` partition, and draws `12202..12204` emit another non-rendered tail. The non-rendered spans have coherent source/UV ranges, so the next investigation should explain why those packet strips clip/degenerate in our phase and whether Dolphin is displaying a different packet phase or material partition order.
- Added `scripts/build-sonic-nonrendered-strip-report.ps1` and wired it into the retail harness after the packet material partition report. The focused report in `artifacts/compat-runs/20260603-235530/sonic-gx-window-12180-provenance-materials/sonic-nonrendered-strips` classifies packet `0x813184D0` as: draws `12181..12187` offscreen-above, draw `12188` rendered dark, draw `12189` offscreen-left, draws `12190..12194` fully clipped, draws `12195..12198` offscreen-right, draws `12199..12201` rendered lit, and draws `12202..12204` offscreen-left. There are no depth/alpha rejection explanations in this packet window; the missing strips are already clipped or outside the diagnostic frame before material sampling matters.
- Added `scripts/build-sonic-packet-placement-overlay.ps1` and wired it into the retail harness after the non-rendered strip report. The tested contact sheet in `artifacts/compat-runs/20260603-235530/sonic-gx-window-12180-provenance-materials/sonic-packet-placement-overlay`, drawn over the neighboring displayed-phase `auto.png`, shows the packet placement mismatch visually: the dark `0x0072C600` partition clamps across the top band, the lit `0x0071BC60` partition occupies the lower visible band, and the remaining packet sequences are edge markers for offscreen-above/left/right or fully-clipped spans. This makes the next target upstream placement/phase comparison, not another material or sampler pass.
- Added `scripts/build-sonic-packet-placement-comparison-report.ps1` and wired it into the retail harness after the placement overlay. The report in `artifacts/compat-runs/20260603-235530/sonic-gx-window-12180-provenance-materials/sonic-packet-placement-comparison` joins packet timeline, scene-state, packet-selection bounds, and non-rendered sequence summaries. For the focused packet `0x813184D0`, it ranks the packet as the largest unique packet in the sampled scene (`radius=4886.078`, rank 1 overall and rank 1 within kind `0x16`), with the next-largest unique packet down at `747.5167`. Only the focus packet is a large-radius packet in the neighbor window; the immediate kind-`0x14` neighbors use small `89.67852` bounds and sit thousands of units away in matrix Z. The new focus-event lifecycle CSV shows the same packet recurring nine times from instruction `26653633` through `49532006` with identical object position, matrix translation, matrix slots, mapped draw range, and radius while the broader scene-state hashes advance each pass. This strengthens the current conclusion that the displayed mismatch is object/scene phase or packet selection context, not projection, TEV, sampler, or local matrix-copy failure.
- Added `scripts/build-sonic-focus-packet-phase-report.ps1` and wired it into the retail harness after the placement comparison report. The report in `artifacts/compat-runs/20260603-235530/sonic-gx-window-12180-provenance-materials/sonic-focus-packet-phase` shows all nine focus-packet events render after the nearest display copy in this bounded replay: display copy `1` is at draw `12180`, while packet `0x813184D0` maps to draws `12181..12204` and contributes the dark/lit material writes after that copy. This means the current focused material diagnostics are post-copy EFB diagnostics for the draw-12180 phase unless the harness captures the next display copy or a longer interval. The next visual-phase target should therefore bracket the display copy after this packet, or explicitly compare current EFB versus copied XFB instead of treating draw `12180` as the final presentation boundary.
- Added `scripts/build-sonic-copy-bracket-report.ps1` plus the `sonic-gx-window-12180-next-copy` benchmark target. The report groups display-copy intervals and marks whether packet `0x813184D0` falls between the previous display copy and the current copy. On the existing narrow provenance run, `sonic-copy-brackets` reports one display copy, zero copies including the focus packet, and zero focus events with a later display copy; that is expected because the artifact stops after the packet's post-copy EFB construction. The new `sonic-gx-window-12180-next-copy` target runs the draw window from `12180` for `3900` draws, dumps a frame, GX copies, coverage, and triangle/material summaries, and supplies the known focus draw range `12181..12204` so the bracket report can answer whether the following display copy actually presents this packet.
- Ran `sonic-gx-window-12180-next-copy` in `artifacts/compat-runs/20260604-021316`. The target completed in `657.314s` and selected display copy `1660` at draw `16058` from XFB address `0x00538460`; the copied frame is full-frame nonblack (`261,173` pixels, `85.017%`, bounds `0/0-639/479`) and the frame lifecycle reports the EFB was cleared afterward. The copy-bracket report now proves packet `0x813184D0` is included by that following display copy: previous display copy draw `12180`, focus packet draws `12181..12204`, next display copy draw `16058`, delta from packet end `3854`. The long window also repeats the material split in the later pass: `0x0072C600` totals `152,704` color writes with black-write ratio `0.596186`, while `0x0071BC60` totals `461,713` color writes with black-write ratio `0.05561`. This means the dark/bright packet content is not merely an unpresented staging pass; it is carried into the displayed city/bridge frame.
- Compared the copied next-copy frame against the Dolphin anchor samples with `scripts/run-sonic-visual-anchor.ps1 -CandidatePath artifacts/compat-runs/20260604-021316/sonic-gx-window-12180-next-copy/auto.png -NoBuild`. The output in `artifacts/sonic-visual-anchor/20260604-022640` still shows a large visual mismatch: full-frame best is Dolphin frame 550 at `85.017%` changed, skyline `76.008%`, bridge `99.425%`, and lower-track `99.975%`. Presentation bracketing is therefore cleared as the cause; the copied frame itself carries the wrong bridge/track composition.
- Added `scripts/build-sonic-scene-state-delta-report.ps1` and wired it into the retail harness for runs with `sonic-scene-state.csv`. Running it on `artifacts/compat-runs/20260603-235530/sonic-gx-window-12180-provenance-materials` compares the nine recurring `0x813184D0` scene-state events from instruction `26653633` through `49532006`. Scalar fields are stable except `instruction`; `packet_bytes`, `object_bytes`, and `mode_pointer_bytes` are byte-identical across all nine events. The changing blobs are `state_bytes` (`5` bytes / `3` words), `small_data_bytes` (`9` bytes / `4` words), `current_matrix_bytes` (`30` bytes / `11` words), and `previous_matrix_bytes` (`29` bytes / `11` words). The compact changing global addresses are `0x801CC1E0`, `0x801CC1E4`, `0x801CC244`, `0x803ADCC0`, `0x803ADCC4`, `0x803ADCD0`, and `0x803ADD20`; the rest of the observed packet/object state is stable. That gives the next writer-trace target list without broad memory watching.
- Added `scripts/build-sonic-scene-writer-event-report.ps1` and `scripts/run-sonic-scene-writer-probe.ps1` for the next provenance step. The probe runs tight `--trace-sonic-input-writes` passes for `0x801CC1E0+0x68` and `0x803ADC80+0xA8`, then the report joins those stores to each repeated `0x813184D0` scene-state event. Its key output is `sonic-scene-writers/scene-writer-to-focus-events.csv`, which names the last writer PC for each changed global before every focus-packet occurrence. The default join targets keep adjacent changed words separate (`0x801CC1E0`, `0x801CC1E4`, `0x801CC244`, `0x803ADC84`, `0x803ADC88`, `0x803ADC94`, `0x803ADCE4`) so a later neighboring clear does not mask the previous writer of the counter/timer word.
- Ran bounded Release writer probes through instruction `30,500,000`. The first combined report in `artifacts/compat-runs/sonic-scene-writer-combined-fulltrace-20260604/sonic-scene-writers` mapped the state globals to stable writers: `0x801CC1E0` from `0x800D3B5C`, `0x801CC1E4` from `0x80BCBFB0`, and `0x801CC244` from `0x80035AE8`. It also exposed a report-labeling bug: `small_data_bytes` is captured from `small_data_state_address & ~0x3F`, not from `small_data_state_address`. The scene-state CSV now records `small_data_window_address`, and the delta/writer scripts label the small-data deltas against the real `0x803ADC00` window base.
- Re-ran the corrected small-data writer probe in `artifacts/compat-runs/sonic-scene-writer-small-data-corrected-20260604`. The corrected delta report shows the small-data changing words are `0x803ADC84`, `0x803ADC88`, `0x803ADC94`, and `0x803ADCE4`. All have ordinary direct writers: `0x803ADC84` from `0x800E8078`, `0x803ADC88` from `0x800E8064`, `0x803ADC94` from `0x800E806C`, and `0x803ADCE4` mostly from the Sonic resource flag/state PCs such as `0x800EBD44`, `0x800EC5A0`, `0x800EBEC8`, `0x800EC944`, and `0x800ECA3C`. The previous `0x803ADCD0` mystery was therefore a diagnostic address-base bug, not an unobserved memory write path.
- Added `scripts/build-sonic-small-data-phase-report.ps1` to decode those corrected small-data words beside each repeated focus-packet event. The report in `artifacts/compat-runs/sonic-scene-writer-small-data-corrected-20260604/small-data-range/sonic-small-data-phase` shows 12 focus events with 7 unique pointer values, 4 unique `0x803ADC88` phase words, 12 timer values, and two flag states. Several later events return to the common `0x80101590` / `0x00130000` pair, so the next A/B probe should isolate the Sonic resource fast-forward family against these decoded phase rows instead of tracing broader memory ranges.
- Added `--disable-sonic-resource-fast-forward` plus individual resource-helper switches for lookup, mode query, state poll, and fixup, and added `-SkipWriterReport` to `scripts/run-sonic-scene-writer-probe.ps1` for long A/B probes. The full resource-disabled 37M run in `artifacts/compat-runs/sonic-scene-writer-small-data-no-resource-ff-37m-20260604` reaches the same 12 focus events after about a `6.15M` instruction offset, but only 4 event-index phase tuples match the fast-forwarded baseline. The shared tuples are the common `0x801027AC/0x00150000` and `0x80101590/0x00130000` states, while the no-resource run introduces `0x800ED370`, `0x801151F4`, and `0x80BC6E8C` phase pointers. Isolating `--disable-sonic-resource-fixup-fast-forward` reproduced the divergence by itself, so the fixup helper is now default-off and only available through `--enable-sonic-resource-fixup-fast-forward`. The root cause is the `0xCA` resource-switch path: when `r29` names a previous resource, the real routine calls side-effect helpers that the old fast-forward counted but did not model. The fast-forward tail also now preloads the next record opcode into `r4` and CR before any interpreter fallback.
- Refreshed the default small-data probe after making the fixup helper default-off. `artifacts/compat-runs/sonic-scene-writer-small-data-default-fixup-off-315m-20260604` is phase-identical to the explicit fixup-disabled probe: 12/12 event-index tuples match, instruction deltas are all zero, and the summary reports `sonicResourceFixupInstructions=0`. The Release visual anchor in `artifacts/sonic-visual-anchor/20260604-140759` also reports `sonicResourceFixupInstructions=0`, but its candidate hash matches the older `20260603-230339` visual anchor exactly and the Dolphin comparison remains poor (`full=85.046%`, `bridge=99.415%`). The fixup correction removes a real phase hazard, but it does not by itself fix the visible Sonic bridge/title mismatch.
- Isolated the resource lookup fast-forward with `artifacts/compat-runs/sonic-scene-writer-small-data-no-resource-lookup-ff-340m-20260604`. Disabling only lookup reaches 12 focus events after about a `2.61M` instruction offset, but only 2 event-index phase tuples match the default baseline and it introduces phase pointers such as `0x801130D8`, `0x80BC717C`, and `0x80BC6E58`. The lookup helper is therefore also default-off and opt-in only through `--enable-sonic-resource-lookup-fast-forward`; continue with mode-query and state-poll isolation next.
- Refreshed the visual anchor with lookup and fixup both default-off in `artifacts/sonic-visual-anchor/20260604-142759`. The candidate hash still matches `20260604-140759` and `20260603-230339`, and the comparison remains unchanged (`full=85.046%`, `bridge=99.415%`), so those phase corrections do not move the current displayed frame. Isolated mode-query in `artifacts/compat-runs/sonic-scene-writer-small-data-no-resource-mode-query-ff-365m-20260604`; disabling only mode-query reaches 13 focus events after about a `1.41M` first-event offset, with 9 event-index tuple matches and shifted phase pointers such as `0x80113A00`, `0x80BC701C`, and `0x80BC6E64`. Mode-query is now default-off and opt-in only through `--enable-sonic-resource-mode-query-fast-forward`; state-poll remains the only default-enabled resource helper to isolate.
- Isolated state-poll in `artifacts/compat-runs/sonic-scene-writer-small-data-no-resource-state-poll-ff-380m-20260604`. Disabling it reaches 13 focus events after about a `2.62M` first-event offset, with 9 event-index tuple matches and phase pointers such as `0x800ED370`, `0x801151F4`, and `0x80BC6E8C`. State-poll is now default-off and opt-in only through `--enable-sonic-resource-state-poll-fast-forward`, so all four Sonic resource fast-forward helpers are compatibility-off by default.
- Refreshed the all-resource-off default in `artifacts/compat-runs/sonic-scene-writer-small-data-default-resource-all-off-380m-20260604`; it is phase-identical to the explicit state-poll-disabled probe with 13/13 event-index tuple matches, zero instruction deltas, and all four resource counters at zero. The all-resource-off Release visual anchor in `artifacts/sonic-visual-anchor/20260604-145039` still hashes exactly the same as the prior anchors and remains mismatched against Dolphin (`full=85.046%`, `bridge=99.415%`). The Sonic resource fast-forward family is now cleared as a visible-frame cause for the current bridge/title mismatch, though leaving it off is still the right compatibility baseline.
- Re-ran `sonic-gx-window-12180-provenance-materials` under the all-resource-off default in `artifacts/compat-runs/20260604-145823/sonic-gx-window-12180-provenance-materials`. `scripts/build-sonic-provenance-comparison.ps1` compares it against the earlier `20260603-235530` provenance run: material summaries and transform-source packet summaries are identical, and the only vertex-provenance mismatches are instruction numbers. Packet-selection and scene-state mismatches are timing/global-state hash churn, not packet/material/source-record content changes. Added `scripts/build-sonic-focus-matrix-provenance.ps1`; it shows focus packet `0x813184D0` still builds its transform from source slot `0xE0000060` into output slot `0xE0000090`, with the nearest source-slot writer at `0x801160DC` / `LR=0x8011E068` exactly 85 instructions before the terminal packet matrix write. The remaining Sonic mismatch is therefore still above local matrix copy/material/transform emission: scene object or material partition state versus Dolphin, not the now-disabled resource fast-forwards.
- Added `scripts/build-sonic-packet-recurrence-report.ps1` and wired it into the retail harness after the focus-packet/copy-bracket reports. It joins focus packet recurrences, copy relation, material partition sequence, coverage totals, and focus matrix provenance into `sonic-packet-recurrence/packet-recurrence-events.csv`. On `20260603-235530`, the focus packet recurs 9 times; on the all-resource-off `20260604-145823` run, it recurs 8 times. Both runs keep the same five-sequence material signature: no-rendered `12181..12187`, dark `0x72C600` at draw `12188`, no-rendered `12189..12198`, lit `0x71BC60` at draws `12199..12201`, and no-rendered `12202..12204`. Every recurrence in these bounded provenance windows is classified `nearest-copy-before-packet`, so this report confirms the repeated packet/material state is stable while the broader scene globals advance. The next higher-value capture should use the long next-copy window with enough provenance to classify the recurrence that is actually included by the following display copy.
- Added `sonic-gx-window-12180-next-copy-provenance`, a long next-copy target with provenance enabled but texture-file dumping disabled so it avoids the several-thousand-file texture dump cost. The completed run in `artifacts/compat-runs/20260604-195431/sonic-gx-window-12180-next-copy-provenance` rebuilds the focus packet timeline with the correct 24-draw span and then uses the long copy interval for display-copy bracketing. `sonic-copy-brackets/focus-packet-copy-brackets.csv` now shows every recurrence of packet `0x813184D0` maps to draws `12181..12204`, is included by display copy `4` at draw `16058`, and that copy has `261,173` nonblack pixels across `0/0-639/479`. `sonic-packet-recurrence/packet-recurrence-events.csv` classifies all 8 recurrences as `included-by-next-display-copy`; all have identical packet-local totals (`307,205` color writes, `58,313` black writes, ratio `0.189818`, color draws `12188..12201`) and the same dark/lit material signature. This clears the last presentation-bracketing ambiguity for the focus packet: the wrong-looking packet content is definitely part of the displayed frame, and it is stable across recurrent scene-state hashes.
- Added `scripts/build-sonic-displayed-packet-set-report.ps1` and wired it into the retail harness for Sonic runs that have triangle coverage, vertex summaries, and packet timeline mapping. On the same long next-copy provenance run, `sonic-displayed-packet-set/displayed-packet-set-report.json` reports `713,669` displayed color writes and `116,717` black writes in the copied interval. The exact packet map currently attributes `307,205` color writes to focus packet `0x813184D0`; the remaining `406,464` color writes are visible but not yet packet-mapped by the current focus provenance. The top materials confirm the repeated dark/lit pair: `0x0072C600` appears at mapped draw `12188` and unmapped draw `16066`, while `0x0071BC60` appears at mapped draws `12199..12201` and unmapped draws `16077..16079`. The next packet-provenance target should anchor the later `16066..16079` material pair so we can identify whether it is the same object packet re-emitted or a different scene packet with matching materials.
- Added and ran `sonic-gx-window-16066-provenance-materials`, a narrow later-pair provenance target anchored at FIFO `+0x37FF4A` with texture-file dumping disabled. The run in `artifacts/compat-runs/20260604-203820/sonic-gx-window-16066-provenance-materials` maps the later material pair exactly: packet `0x813184D0`, object `0x813184E8`, packet kind `0x16`, mapped draw range `16059..16090`, and FIFO range `+0x37FCDD..+0x3815FA`. Its displayed packet report attributes all `307,212` color writes in the narrow window to the same focus packet: dark `0x0072C600` at draw `16066` and lit `0x0071BC60` at draws `16077..16079`. This proves the long copied frame contains two visible emissions of the same large focus packet, not two different packets with matching materials.
- Added `scripts/build-sonic-packet-emission-comparison.ps1` to compare two mapped packet-emission windows by recurrence instruction. Comparing the earlier `12180` window against the later `16066` window joins all 8 focus-packet recurrences. All 8 have equal packet hash, object hash, matrix translation, and material identity after normalizing only draw ranges and coverage counters. Every joined recurrence shifts from draws `12181..12204` to `16059..16090` (`draw_start_delta=3878`, `draw_end_delta=3886`) and from FIFO `+0x2D1E3F..+0x2D2FE1` to `+0x37FCDD..+0x3815FA` (`fifo_start_delta=+0xADE9E`, `fifo_end_delta=+0xAE619`). The later window also maps the adjacent kind-`0x14` packet `0x81318FC8` / object `0x81318FE0` at handoff draw `16090`, so the next useful probe is the scene/object traversal that emits this packet pair twice rather than packet-local rendering.
- Added `scripts/build-sonic-packet-traversal-pair-report.ps1` to group raw packet-selection rows into complete dispatches and pair packet `0x813184D0` with the next `0x81318FC8` dispatch. On `20260604-203820`, the available raw trace contains two focus/neighbor pairs. Both pairs use the same phase sequence (`callback_wrapper_entry`, `callback_wrapper_after_flush`, `callback_dispatch_call`, `primary_wrapper_entry`, `primary_wrapper_call`, `renderer_entry`), the same callback target `0x80100E20`, and the same renderer entry `0x8011D414`. The neighbor follows the focus packet by exactly `16,752` instructions in both captured pairs. This keeps the boundary above the wrapper/render dispatch path: the duplicate visible bridge packet is coming from the scene traversal/list contents that feed the wrapper, not from a packet-local rendering branch or culling helper.
- Current Sonic rendering boundary: the renderer now produces stable pixels and material summaries for a display-copy phase that includes two visible emissions of packet `0x813184D0`. Presentation bracketing is cleared, both dark/bright material groups belong to the same object packet and overlapping source geometry, and the non-rendered strips classify as clipped or offscreen rather than depth/alpha rejection. The traversal-source report in `artifacts/compat-runs/20260605-012608/sonic-gx-window-16066-provenance-materials/sonic-traversal-source-report` captures seven focus/neighbor recurrences; all use list base `0x813660F8`, traversal target `0x80BC7D6C`, renderer target `0x80100E20`, and the same `16,752`-instruction focus-to-neighbor delta. This clears divergent wrapper/list callers as the duplicate-emission cause.
- The software renderer now clips view-space triangles against left/right/top/bottom clip planes in addition to near/far/W. A right-frustum regression test prevents positive offscreen projected geometry from clamping back into the framebuffer. Re-running `sonic-gx-window-16066-provenance-materials` in `artifacts/compat-runs/20260605-012608` reduced focus-packet color writes from `307,212` to `223,598`; the packet is still displayed, but the oversized bridge overdraw is materially reduced. Re-running the long copied-frame target in `artifacts/compat-runs/20260605-013437/sonic-gx-window-12180-next-copy` now selects display copy `1660`/draw `16058` with `159,252` nonblack pixels (`51.84%`, bounds `90/99-639/479`) instead of the prior full-frame `261,173` pixels (`85.017%`, bounds `0/0-639/479`). The visual-anchor script now emits `visible-best-summary.csv`/`visible-contact-sheet.png` by parsing baseline nonblack ratios, with optional `-VisibleSampleMinFrame`/`-VisibleSampleMaxFrame` filters. The raw metric in `artifacts/sonic-visual-anchor/20260605-014427` still picks black Dolphin frame 550, while the city-filtered visible metric (`-VisibleSampleMinFrame 600`) picks Dolphin frame 700 for full/skyline (`74.342%` and `63.608%` changed), frame 1500 for bridge (`89.876%`), and frame 1100 for lower-track (`97.758%`). Visually, the candidate bridge/city render is plausible; the Dolphin sheet is showing Shadow while the candidate is showing the bridge, so treat the large difference as a sub-second sync/reference alignment issue first, not as evidence that the bridge render itself is bad. The remaining mismatch is therefore timing/presentation selection and sequence alignment rather than the earlier frustum-clamped full-screen overdraw.
- Added `scripts/run-sonic-sync-sweep.ps1`, a sync-aware comparison harness that can either reuse an existing candidate PNG directory or run a GX frame sweep, compare each candidate against a Dolphin sample window, filter both Dolphin and candidate images by nonblack percentage, and emit `sync-sweep-summary.csv`, `best-by-candidate.csv`, `best-overall.csv`, and contact sheets. Generated a dense Sonic Dolphin reference in `artifacts/dolphin-reference/20260605-065322` with real frames `600..1200` at 10-frame spacing after the first shorter attempt only reached actual frame 545. Reusing the clipped copied-frame candidate against that dense reference (`artifacts/sonic-sync-sweep/20260605-065520`) finds the best full-frame match at Dolphin frame `1130` (`52.821%` changed), but visual inspection still shows black/Shadow-frame bias in the whole-frame metric. A candidate-visible filtered sweep over the generated GX slices (`artifacts/sonic-sync-sweep/20260605-070455`) isolates the meaningful bridge candidate at skip draw `12320`, best against Dolphin frame `690` (`63.607%`). A tighter sweep around the known copy window (`artifacts/sonic-sync-sweep/20260605-070807`) shows skip draws `12160` and `12180` preserve the fuller bridge/city frame (`51.84%` candidate nonblack), while the best filtered narrow candidate is skip `12380` vs Dolphin frame `690` (`61.075%`). This reinforces the sync interpretation: the renderer is producing plausible bridge/city imagery, and the next work should compare/correct VI/display-copy timing and harness selection against the dense frame window rather than chasing TEV/texture output first.
- Extended `--dump-gx-frame-sweep` summaries with selected-copy lifecycle metadata: source copy index, selected copy index/kind/draw/FIFO/address, last display copy, copy/draw distance from the last display copy, clear/texture-copy counts since the last display copy, and lifecycle phase. `scripts/run-sonic-sync-sweep.ps1` now propagates those fields into its comparison CSVs and contact sheet labels. The fresh three-slice validation in `artifacts/sonic-sync-sweep/20260605-072111` and the labeled reuse pass in `artifacts/sonic-sync-sweep/20260605-072534` show skip draws `12160`, `12180`, and `12200` all select display copy/source copy `1660`, emitted at draw `16058`, FIFO `+0x37F74F`, XFB address `0x00538460`. This pins the bridge candidate to one concrete display copy; the remaining sync task is to map that copy to the corresponding VI/presentation frame cadence rather than guessing from image paths.
- Added `scripts/build-vi-display-timeline-report.ps1` plus a streaming Node helper for large MMIO traces, and wired the report into the retail harness for MMIO-enabled copy runs. The Sonic VI timeline target in `artifacts/compat-runs/20260605-075906/sonic-gx-window-12180-next-copy-vi-timeline` maps display copy `1` at draw `12180` to XFB `0x004A2460`, then VI field0 first displays it `71,672` instructions later. The bridge/city copy `4` at draw `16058` writes XFB `0x00538460`, sees VI still pointing at the previous `0x004A2460/0x004A2960` field pair at copy time, and is first displayed by field0 `71,747` instructions later. That confirms the candidate frame is following normal double-buffered VI cadence; it is not an off-screen copy or a stale screenshot-selection accident.
- Added `scripts/build-sonic-vi-sync-report.ps1` to join sync-sweep candidates with the VI display-copy timeline, and `scripts/run-sonic-sync-sweep.ps1 -ViTimelineDirectory <dir>` now generates it automatically. The report in `artifacts/sonic-sync-sweep/20260605-072534/vi-sync-report`, plus the hook validation in `artifacts/sonic-sync-sweep/20260605-103046/vi-sync-report`, classify the current sweep as `vi-displayed-stable-sync-window`: all three candidates select the same XFB copy (`draw=16058`, FIFO `+0x37F74F`, address `0x00538460`), that copy is displayed by VI field0 `71,747` instructions after copy time, and the filtered Dolphin match is stable at frame `690`. The best row is currently `gx-frame-skip-012200` vs Dolphin `frame-00690-requested-00690` (`66.093%` changed), but its lower nonblack coverage means this is a metric/visibility tradeoff rather than a different presentation copy.
- Added `scripts/build-sonic-frame-delta-report.ps1` for same-frame candidate/sample classification masks and region coverage metrics. Running it against the visually correct bridge reference, Dolphin `frame-00680-requested-00680`, with candidate `gx-frame-skip-012160` writes `artifacts/sonic-frame-delta/20260605-113817`: the full-frame result is `candidate-missing-sample-coverage`, with sample nonblack `77.495%`, candidate nonblack `51.840%`, shared nonblack `49.383%`, sample-only `28.112%`, candidate-only `2.456%`, and nonblack Jaccard `0.618`. The left-side region is the clearest failure (`63.420%` sample-only, `0%` candidate-only), meaning the remaining bridge mismatch is missing/over-clipped background/left coverage rather than extra candidate geometry. The pre-copy draw attribution for the selected XFB interval `12181..16058` still has the expected focus bridge textures (`0x0072C600` at draw `12188`, `0x0071BC60` at draws `12199..12201`) plus broader city/background CMPR groups, so the next target should be the large background/sky/city draw coverage before copy `16058`, not the post-copy repeated packet at `16066`.
- Added `scripts/build-sonic-background-attribution-report.ps1` to focus on the missing stars and distant skyline/building layers. The crop masks from `artifacts/sonic-background-attribution/20260605-181822` show the top sky is almost entirely Dolphin-only coverage: the Dolphin crop has a full star field, while the candidate crop is mostly black with only sparse lower-right star pixels. The fast GX attribution run in `artifacts/sonic-background-attribution/20260605-182235` shows those regions are not completely unvisited: top-sky has 11 rendered triangle rows with `105,145` color writes, but the dominant `0x0072C600` bridge material at draw `12188` contributes `71,780` color writes with `81.957%` black writes; left-sky similarly has `0x0072C600` at `92.488%` black writes. The Empire-building/right-city regions are mostly rendered by `0x0071BC60` and later city CMPR groups with low black ratios, so the missing stars look more like background/sky coverage being absent or overwritten/occluded than a universal texture decode failure.
- Added the warmed Sonic copy target `sonic-gx-window-16058-warmed-display-copy`, which starts replay at draw `8302`, forces display copy `1660` at draw `16058`, skips copy memory writes, and avoids coverage output for faster visual checks. Direct probe runs in `artifacts/sonic-warmed-copy-probe/20260605-193539` and `artifacts/sonic-warmed-copy-probe/20260605-193925-copy1660` show that the forced warmed copy is byte-identical to the earlier sync-sweep candidate `gx-frame-skip-012160.png` (`SHA256 6DF7B0BFEBB26A44FB5C2088B074C4CCB08DC7B1DF1C4137814B0FA710FDC014`), so the missing left/top-left background is not a late-replay diagnostic artifact. The frame-delta sheet in `artifacts/sonic-frame-delta/20260605-194216-warmed-copy1660/20260605-194217` still classifies the full frame as `candidate-missing-sample-coverage` against Dolphin frame `680` (`77.495%` sample nonblack, `51.840%` candidate nonblack, `49.383%` shared, Jaccard `0.618`), with the worst remaining defect on the left side (`left-void` Jaccard `0.285`). The next renderer task is therefore the left/top-left sky and distant skyline layer, not the exact display-copy/VI selection or replay-start selection.
- Fixed the Sonic left/top/right sky and distant-city coverage regression in the software renderer. The root cause was projected screen-space GX vertices near `0..1` being routed through the legacy NDC coordinate remap after clipping, which warped clip intersections toward the center of the EFB. Projected vertices now stay in pixel coordinates, clipped polygons are projected after clip-plane processing, and canonical 640x480 viewport draws can rely on the rasterizer's framebuffer clamp for screen-edge triangles. The proof render in `artifacts/sonic-render-probe/20260606-current-copy1660-canonical-screen-raster-fix/auto.png` restores the starfield, distant skyline, Empire State building region, and right-side city to match the earlier good copy-source reference. A focused regression test, `ProjectedScreenSpaceCoordinatesRemainPixelCoordinates`, guards the coordinate path while the existing right-frustum test still protects noncanonical viewport clipping.
- Post-fix Release sweep `artifacts/compat-runs/20260606-133509` re-ran Sonic's canonical warmed copy-source target, Pikmin 20M, and Mario Kart Double Dash debug 20M. Sonic selected copy-source `1660` at XFB `0x00538460` and produced `auto.png` SHA-256 `710c90b7546ec5f2d82d6a9fd6fb336b15d8b6a3e5bd10229ed9b27906d24c6d`, byte-identical to both the standalone proof render and the earlier good `20260605-213021` copy-source reference. Pikmin 20M still reaches max instructions and emits a largest-display-copy frame from copy `2`. Double Dash debug 20M reaches max instructions with zero GX FIFO bytes, so its requested GX frame dump exits with diagnostic failure `gx-frame-dump: no GX FIFO writes were captured`; the no-frame `mariokart-debug-5m` smoke in `artifacts/compat-runs/20260606-133955` completes normally.
- Extended Double Dash debug first-FIFO probing through `artifacts/mariokart-debug-probe/20260606-first-gx-250m`: the run reached 250M interpreted instructions with zero GX FIFO bytes, no pending DI command, DI status `0x2A`, and no pending external interrupt. Exact fast-forwards already skipped roughly 73.1M leaf-helper instructions, 36.6M string-length instructions, 6.5M memory-copy instructions, and 1.9M cache instructions, so the current Double Dash debug wall is game/debug-build bootstrap volume rather than an obvious missing DVD/EXI interrupt. Run summaries now include `pcProfileWithoutFastForwardLeaves` to make these long probes point past already-modeled leaf stubs.
- Inspect the remaining Sonic GX setup/profile window around `0x80101948`; the clean-path helper intentionally refuses dirty-state flushes, so any next fast-forward there needs to model the observed state flush helper calls and FIFO writes.
- Analyze the `0x80027D3C..0x80027E68` nested scan carefully before considering any exact fast-forward; it has enough control flow that a subsystem/CPU correctness bug would be easier to hide.
- Inspect allocator/accounting behavior around `0x80046FE4..0x8004713C`; if it turns into a hot loop, prefer implementing the missing lower-level memory/runtime behavior over broad allocation fast-forwards.
- For the current `0x801B12EC..0x801B138C` tail, trace the virtual target at `lwz r12, 52(r12)` and improve the underlying behavior it calls; do not skip the list walk wholesale because each callback may mutate gameplay/object state.
- Improve presentation selection so Sonic chooses the largest/latest useful XFB copy for user-facing screenshots without hiding the later black-overwrite lifecycle in diagnostics.
- Promoted the Sonic resource-fixup fast-forward back to default-on after tightening its unsafe `0xCA` path: if `r29` names a previous resource, the helper now refuses and falls back to the interpreter instead of skipping side-effect calls. The same Sonic 20M perf/profile slice improved from `81.820s` / `0.247` emulation MIPS at `artifacts/compat-runs/20260611-121452` to `66.679s` / `0.305` emulation MIPS at `artifacts/compat-runs/20260611-121734`, and advanced the final PC from `0x8011DEE8` to `0x801013B4` while reporting `sonicResourceFixupInstructions=916775`. Opcode-level tests now cover the safe record mutations and the safe `0xCA` resource switch; `--disable-sonic-resource-fixup-fast-forward` remains available for A/B checks, while lookup/mode-query/state-poll remain opt-in.
- Extended the Sonic null-slot scan fast-forward to skip non-null slots whose object id does not match the scan target, while still stopping before the first matching slot. The parity test now covers a mixed mismatch/null/match path and verifies the final `r0`, `r5`, CR, CTR, decrementer, and cursor state against the interpreter. Sonic 20M perf/profile improved again from the default-fixup baseline `42.496s` / `0.479` emulation MIPS in `artifacts/compat-runs/20260611-122238` to `36.098s` / `0.566` emulation MIPS in `artifacts/compat-runs/20260611-122715`, with the final PC moving to `0x80BC6E54` and `sonicNullSlotScanInstructions=2606712`. The old `0x80116BBC` entry cluster dropped out; remaining `0x80116Cxx` samples are matching/follow-up paths and need disassembly-backed modeling before any further shortcut.
- Added exact fast-forwards for the two Sonic pool-slot scans at `0x80116C18` and `0x80116C8C`. They skip occupied/non-sentinel records only, preserve the last loaded/compared registers and CR, and stop before the first free/sentinel record that performs stores or changes selection state. `summary.csv` now flattens `sonicPoolSlotScanInstructions`. The first Sonic 20M validation in `artifacts/compat-runs/20260611-124707` reports `sonicPoolSlotScanInstructions=378792`, removes the `0x80116Cxx` clusters from the top profile, and moves the final PC to `0x801034D0`; wall time was `41.109s` / `0.496` emulation MIPS, so treat it as a compatibility-progress win more than a clean timing win until repeated on an idle machine.
- Added an exact table-key miss-scan fast-forward for Sonic's `0x801190B8` loop. It reads the table base from `0(r31)` like the interpreter, skips stride-12 records whose low 16-bit key does not match `r29`, and stops before the matching record that feeds the real handler call. The focused parity test covers both pre-hit and exhausted paths. `summary.csv` now flattens `sonicTableKeyScanInstructions`; the Sonic 20M validation in `artifacts/compat-runs/20260611-132456` reports `sonicTableKeyScanInstructions=131049`, removes `0x801190B8..0x801190D8` from the top clusters, reaches final PC `0x80103590`, and improves this run to `30.241s` / `0.674` emulation MIPS.
- Added a narrow Sonic mode-refresh dispatch fast-forward for the `0x80123478` call shell around the existing mode-wrapper helper. It folds only the pointer-load/call setup and post-call `or/cmp/bne` branch, with parity coverage for the call path plus zero/nonzero return paths. `summary.csv` now flattens `sonicModeRefreshDispatchInstructions`; the Sonic 20M validation in `artifacts/compat-runs/20260611-133339` reports `sonicModeRefreshDispatchInstructions=84480` and advances the endpoint from `0x80103590` to `0x80124358` while keeping the table/pool/resource-fixup counters stable. Wall time varied heavily under system load, so use endpoint movement and counters more than this pass's elapsed seconds.
- Improved Sonic profiling hygiene and the resource-flag wait helper. `pcProfileWithoutFastForwardLeaves` now filters known exact Sonic fast-forward roots, and the resource flag wait fast-forward covers the `0x800EBEA4` compare tail and `0x800EBEA8` branch tail in addition to the original `0x800EBEA0` entry. The validation in `artifacts/compat-runs/20260611-134157` keeps the good `0x80124358` endpoint, reports the same large `sonicResourceFlagWaitInstructions=798173502`, and removes `0x800EBEA4..0x800EBEA8` from the filtered profile. A follow-up A/B wiring of the narrow status-caller dispatch regressed the endpoint to `0x800E3170` in `artifacts/compat-runs/20260611-134559`; that result is superseded by the split-dispatch activation below.
- Extended the Sonic mode-refresh dispatch fast-forward to cover the callback-null prelude at `0x801233E0` and its `0x801233E4` / `0x801233E8` tail entries. The helper still refuses the non-null callback path and leaves the mode classifier interpreted. Sonic 20M validation in `artifacts/compat-runs/20260611-141655` reports `sonicModeRefreshDispatchInstructions=90738` and advances the endpoint to `0x8012F708`. An attempted common-path classifier fold at `0x8012340C` over-amplified the loop, reported `63498560` skipped instructions, and regressed the endpoint to `0x80123478` in `artifacts/compat-runs/20260611-141455`, so that fold was removed.
- Added two exact runtime fast-forwards from the next Sonic profile slice: a generic `lwz r3,d(r13); blr` small-data getter leaf and Sonic's ASCII case-insensitive string-compare loop at `0x80072314`. The small-data getter validation in `artifacts/compat-runs/20260611-143603` filters the hot `0x800F3710` getter out of the leaf profile and moves the Sonic 20M endpoint to `0x8012F77C`. After correcting the real loop-back branch signature to `0x4082FFAC`, the string-compare helper validation in `artifacts/compat-runs/20260611-144413` reports `stringCompareInstructions=173002`, removes `0x80072314..0x80072328` from the filtered profile, and reaches endpoint `0x80102F94` under the same 20M budget. Treat the endpoint change as "more guest work within the fixed budget" rather than a visual milestone until paired with a frame capture.
- Added a branch-taken-only Sonic mode-refresh counter check fast-forward for `0x801233EC/F0/F4`. It models the full `bl` into the `0x800F3710` small-data getter, preserves the branch-tail CR when entering at `0x801233F4`, and refuses the rare callback fall-through path. Sonic 20M validation in `artifacts/compat-runs/20260611-145257` reports `sonicModeRefreshDispatchInstructions=150523`, removes `0x801233EC..0x801233F4` from the filtered profile, and reaches endpoint `0x8011CC00` under the fixed budget.
- Activated the split Sonic status-caller dispatch fast-forward for `0x80012B24`, `0x80012B2C`, `0x80012B54`, and `0x80012B58`, leaving the broader whole-loop helper dormant after its timeout in `artifacts/compat-runs/20260612-115036`. The split helper only folds caller setup/branch edges around the existing status-query and mode-coordinator helpers, with focused parity coverage already in place. The Sonic 20M perf/profile validation improved from `281.922s` / `0.071` emulation MIPS in `artifacts/compat-runs/20260612-115907` to `49.014s` / `0.412` emulation MIPS in `artifacts/compat-runs/20260612-120444`, reporting `sonicStatusCallerDispatchInstructions=18955967`. The fixed 20M endpoint remains inside the same caller loop because skipped guest instructions still count toward the instruction budget; treat this as a development-throughput win rather than a new visual milestone.
- Added two conservative Sonic bootstrap/profile helpers: a dispatch-only fold for the `0x800E1158..0x800E1178` table byte-build loop, leaving its mutating classifier at `0x800E1F14` interpreted, and a guarded batch helper for the `0x8013B430..0x8013B45C` line-copy normalizer that copies ordinary bytes until CR/LF/NUL. Focused interpreter-parity tests cover both helpers, and the full suite is now 503 tests. Sonic 20M validation in `artifacts/compat-runs/20260612-162226` reports `sonicTableByteBuildDispatchInstructions=30807` and `sonicLineCopyInstructions=22617`; the filtered profile now moves on to `0x8012340C..0x80123424` and `0x8016F978..0x8016F9BC`. Wall time was load-sensitive on this pass, so use counters/profile movement rather than elapsed seconds as the signal.
- Added a guarded fast-forward for the two Sonic halfword checksum loops at `0x8016F978` and `0x8016FBA8`. The helper consumes only the unrolled no-store body, preserves the last loaded halfword/inverse registers, advances the source pointer, clears CTR, and leaves each loop's remainder/comparison tail interpreted. Focused parity tests now cover both variants and the full suite is 504 tests. Sonic 20M validation in `artifacts/compat-runs/20260612-164323` reports `sonicHalfwordChecksumInstructions=69496`; the checksum clusters are filtered out and the next visible profile heads are `0x8012340C..0x80123424`, `0x8013A39C..0x8013A3B0`, and `0x800F982C..0x800F9854`.
- Added five more conservative Sonic bootstrap/profile folds: the `0x8013A39C` text line-skipper, the `0x8010DF90` string-append destination scan, the `0x80139C94` allocator free-block scan, the `0x800E4F88` cache store sweep, and the `0x800F982C` state zero-fill loop. Each helper has an interpreter-parity test and the full suite is now 509 tests. Sonic 5M validation in `artifacts/compat-runs/20260612-193316` reports `sonicLineSkipInstructions=19368`, `sonicStringAppendScanInstructions=6081`, `sonicFreeBlockScanInstructions=8239`, `sonicCacheStoreSweepInstructions=4096`, and `sonicStateZeroFillInstructions=9856`; those clusters disappear from the filtered profile, leaving the known-risky `0x8012340C..0x80123424`, the table-byte classifier at `0x800E1F14`, and the smaller `0x80124F50..0x80124F64` cluster. Sonic 20M validation in `artifacts/compat-runs/20260612-193409` completed successfully at the max-instruction stop; after instruction 10M, the filtered profile contains only known fast-forward leaves.
- Added three narrow Sonic 16-slot scan folds that consume only inactive entries and stop before active callback/body work: `0x80124F50` manager slots, `0x801303B0` task entries, and `0x801338DC` object slots. Focused interpreter-parity tests cover all-inactive, active-stop, and tail-exit paths; the full suite is now 512 tests. Sonic 5M validation in `artifacts/compat-runs/20260612-201448` reports `sonicManagerSlotScanInstructions=5376`, `sonicTaskEntryScanInstructions=6144`, and `sonicObjectSlotScanInstructions=6144`; those clusters disappear from the filtered profile, leaving `0x801363D0..0x80136480` as the next small non-mode-refresh group after the known-risky `0x8012340C..0x80123424` and `0x800E1F14`. Sonic 20M validation in `artifacts/compat-runs/20260612-201533` completed successfully at the max-instruction stop, with the post-10M filtered profile still fully consumed by known fast-forward leaves.
